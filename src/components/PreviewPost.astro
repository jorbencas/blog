---
const { post } = Astro.props;
const { title, description, pubDate, image } = post.data;

const date = new Date(pubDate).toLocaleDateString("es-ES", {
  year: "numeric",
  month: "short",
  day: "2-digit",
});

const tags = post.data.tags || [];
---

<a href={`/posts/${post.id}`} class="group block h-full">
  <div
    class="relative h-full bg-white dark:bg-slate-900/40 overflow-hidden
 transition-all duration-500 hover:-translate-y-2
 hover:shadow-[0_20px_50px_rgba(6,182,212,0.15)] flex flex-col border border-slate-200 dark:border-transparent"
  >
    <!-- Imagen con Aspect Ratio controlado -->
    <div class="relative aspect-[16/9] overflow-hidden">
      <div
        class="absolute inset-0 bg-gradient-to-t from-slate-950 via-transparent to-transparent z-10 opacity-70"
      >
      </div>

      {
        image ? (
          <img
            src={image}
            alt={title}
            class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110 opacity-80 group-hover:opacity-100"
          />
        ) : (
          <div class="w-full h-full bg-slate-200 dark:bg-slate-800 flex items-center justify-center">
            <span class="text-slate-500 dark:text-slate-600 font-mono text-xs sm:text-sm">
              SIN_IMAGEN
            </span>
          </div>
        )
      }

      <!-- Badge de Fecha flotante -->
      <div class="absolute bottom-3 left-4 z-20 bg-white dark:bg-slate-900">
        <time
          class="text-xs sm:text-sm font-mono text-cyan-500 dark:text-cyan-400 uppercase tracking-[0.2em] font-black bg-white dark:bg-slate-950 px-2 py-1 rounded-sm border border-cyan-500"
        >
          {date}
        </time>
      </div>
    </div>

    <!-- Contenido del Post -->
    <div class="p-5 flex-1 flex flex-col space-y-3 bg-white dark:bg-slate-900">
      <div class="flex items-center gap-2">
        <span
          class="w-1.5 h-1.5 rounded-full bg-cyan-500 shadow-[0_0_8px_rgba(6,182,212,0.8)]"
        ></span>
        <span
          class="text-xs sm:text-sm font-mono text-slate-500 uppercase tracking-widest"
          >Tipo_Entrada: DevLog</span
        >
      </div>

      <h3
        class="text-lg font-black uppercase italic tracking-tighter text-slate-900 dark:text-white group-hover:text-cyan-500 dark:group-hover:text-cyan-400 transition-colors leading-tight"
      >
        {title}
      </h3>

      <p
        class="text-sm sm:text-base text-slate-500 dark:text-slate-400 line-clamp-2 leading-relaxed font-medium italic opacity-80"
      >
        "{description}"
      </p>

      <!-- Botón de lectura unificado -->
      <div class="mt-auto pt-4 flex items-center justify-between">
        <div
          class="flex items-center gap-2 text-cyan-600 dark:text-cyan-500 transition-all duration-300 group-hover:gap-4"
        >
          <span class="text-xs sm:text-sm font-black uppercase tracking-[0.2em]"
            >Leer_Entrada</span
          >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            class="h-3.5 w-3.5 transition-transform group-hover:scale-110"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2.5"
              d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
          </svg>
        </div>

         {tags.length > 0 && (
          <div class="flex flex-wrap gap-2">
            {tags.map((tag: string) => (
              <div
                class="bg-gradient-to-r from-sky-800 to-cyan-500 px-3 py-1 text-xs sm:text-sm font-bold uppercase tracking-wider text-white"
              >
                {tag}
              </div>
            ))}
          </div>
        )}
      </div>
    </div>

    <!-- El mismo efecto Shimmer que los demás -->
    <div
      class="absolute top-0 left-[-100%] w-1/2 h-full bg-gradient-to-r from-transparent via-white/5 to-transparent skew-x-[-25deg] group-hover:left-[150%] transition-all duration-1000 pointer-events-none"
    >
    </div>
  </div>
</a>
