---
draft: false
title: "Docker: ffmpeg y yt-dlp en Windows (WSL) y Ubuntu"
description: "Guía práctica para usar ffmpeg y yt-dlp con Docker en WSL y Ubuntu."
pubDate: "2026-07-11"
tags: ["docker", "devops", "linux", "windows", "ffmpeg", "yt-dlp"]
image: "img/docker_to_yt_ffmpeg_in_wls/docker_to_yt_ffmpeg_in_wls_cover-1200.webp"
author: "Jorge Beneyto Castelló"
---

## 1. El docker-compose.yml adaptado

Lo ideal es que guardes los vídeos directamente en tu disco de Windows <code>(C:)</code> para que puedas abrirlos con el reproductor de Windows sin tener que buscarlos dentro de la máquina virtual de WSL. En WSL, tu disco <code>C:</code> está montado en la ruta <code>/mnt/c/</code>. Crea este archivo docker-compose.yml dentro de tu terminal de WSL:

```yaml
services:
  downloader:
    image: alpine:latest
    container_name: yt_ffmpeg_downloader
    volumes:
      - /mnt/c/Users/TuUsuario/Videos/test_videos:/test_videos
    working_dir: /test_videos
    user: root
    tty: true          # Equivalente a -t en docker run
    stdin_open: true   # Equivalente a -i en docker run
    command: >-
      sh -c "apk update &&
      apk add --no-cache ffmpeg python3 curl bash su-exec &&
      adduser -D -u 1000 tu_usuario &&
      curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o /usr/local/bin/yt-dlp &&
      chmod a+rx /usr/local/bin/yt-dlp &&
      su-exec tu_usuario bash /test_videos/midu.sh &&
      su-exec tu_usuario sh"
```

> **Sobre `tty` e `stdin_open`:** Estas dos opciones equivalen a pasar `-it` al `docker run`. Sin ellas, el contenedor no mantendría la terminal interactiva y no podrías escribir comandos dentro.

> **Sobre los permisos (`user: root` y `su-exec`):** Por defecto Alpine ejecuta todo como `root`, lo que hace que los archivos creados en los volúmenes queden como `root:root`. La solución es mantener `user: root` para instalar paquetes (solo root puede escribir en `/usr/local/bin/`), pero usar `su-exec` para ejecutar el script y la shell como tu usuario real. `adduser -D -u 1000 tu_usuario` crea el usuario dentro del contenedor con el mismo UID que tu usuario del host. Cambia `tu_usuario` por el nombre de tu usuario en Linux (lo puedes ver ejecutando `whoami` en tu terminal).

> **Ojo al detalle:** Recuerda cambiar `TuUsuario` por el nombre real de tu usuario en Windows (lo puedes ver en la terminal de WSL escribiendo `ls /mnt/c/Users/`).

## 2. El script de conversión (midu.sh)

Crea un archivo `midu.sh` en la misma carpeta que el docker-compose.yml. Conversor CLI con soporte para presets de calidad, múltiples códecs de audio, detección de GPU y progreso en tiempo real.

```bash
#!/bin/bash

VERSION="2.0.0"

show_help() {
    cat <<EOF
midu.sh v${VERSION} — Conversor de vídeo a mp4 con ffmpeg

Uso: ./midu.sh [opciones] [directorio]

Opciones:
  -i, --input DIR      Directorio de entrada (default: ./test)
  -o, --output DIR     Directorio de salida (default: ./optimizados)
  -p, --preset PRESET  Calidad: web|default|archive|quality (default: default)
  -a, --audio CODEC    Códec de audio: aac|copy|opus|mp3|flac|vorbis (default: aac)
  -b, --bitrate BR     Bitrate de audio (default: 128k)
  -r, --resolution RES Resolución de salida: original|4k|1080|720|480|360 (default: original)
  -m, --max-size MB    Tamaño máximo de salida en MB (default: sin límite)
                       Calcula el bitrate automáticamente según la duración.
  -t, --threads N      Hilos máximos en paralelo (default: nproc)
  -e, --extensions EXT Extensiones separadas por coma (default: avi,webm,mkv,mp4,flv)
  -h, --help           Muestra esta ayuda
  -v, --version        Versión

Ejemplos:
  ./midu.sh                           # Modo default
  ./midu.sh -m 2000                    # Todo menos de 2GB
  ./midu.sh -m 100 -r 720              # Todo menos de 100MB, 720p
  ./midu.sh -p web -a opus -b 96k      # Web, audio opus a 96k
  ./midu.sh -r 1080                    # Todo a 1920x1080
  ./midu.sh -r 720 -p web              # Todo a 720p, modo web
  ./midu.sh -i /videos -o /salida      # Rutas personalizadas
EOF
}

INPUT_DIR="/test_videos/test"
OUTPUT_DIR="/test_videos/optimizados"
PRESET="default"
AUDIO_CODEC="aac"
AUDIO_BITRATE="128k"
RESOLUTION="original"
MAX_SIZE=""
MAX_THREADS=$(nproc)
EXTENSIONS="avi,webm,mkv,mp4,flv"

while [[ $# -gt 0 ]]; do
    case "$1" in
        -i|--input)     INPUT_DIR="$2"; shift 2 ;;
        -o|--output)    OUTPUT_DIR="$2"; shift 2 ;;
        -p|--preset)    PRESET="$2"; shift 2 ;;
        -a|--audio)     AUDIO_CODEC="$2"; shift 2 ;;
        -b|--bitrate)   AUDIO_BITRATE="$2"; shift 2 ;;
        -r|--resolution) RESOLUTION="$2"; shift 2 ;;
        -m|--max-size)  MAX_SIZE="$2"; shift 2 ;;
        -t|--threads)   MAX_THREADS="$2"; shift 2 ;;
        -e|--extensions) EXTENSIONS="$2"; shift 2 ;;
        -h|--help)      show_help; exit 0 ;;
        -v|--version)   echo "midu.sh v${VERSION}"; exit 0 ;;
        -*)             echo "Opción desconocida: $1"; show_help; exit 1 ;;
        *)              INPUT_DIR="$1"; shift ;;
    esac
done

IFS=',' read -ra EXT_ARRAY <<< "$EXTENSIONS"

get_resolution_filter() {
    case "$1" in
        original) echo "" ;;
        4k|2160)   echo "-vf scale=3840:2160:force_original_aspect_ratio=decrease,pad=3840:2160:(ow-iw)/2:(oh-ih)/2" ;;
        1440)      echo "-vf scale=2560:1440:force_original_aspect_ratio=decrease,pad=2560:1440:(ow-iw)/2:(oh-ih)/2" ;;
        1080)      echo "-vf scale=1920:1080:force_original_aspect_ratio=decrease,pad=1920:1080:(ow-iw)/2:(oh-ih)/2" ;;
        720)       echo "-vf scale=1280:720:force_original_aspect_ratio=decrease,pad=1280:720:(ow-iw)/2:(oh-ih)/2" ;;
        480)       echo "-vf scale=854:480:force_original_aspect_ratio=decrease,pad=854:480:(ow-iw)/2:(oh-ih)/2" ;;
        360)       echo "-vf scale=640:360:force_original_aspect_ratio=decrease,pad=640:360:(ow-iw)/2:(oh-ih)/2" ;;
        *)         echo "Resolución desconocida: $1"; exit 1 ;;
    esac
}

case "$PRESET" in
    web)      CRF=28; SPEED="fast" ;;
    default)  CRF=23; SPEED="medium" ;;
    archive)  CRF=18; SPEED="slow" ;;
    quality)  CRF=15; SPEED="veryslow" ;;
    *)        echo "Preset desconocido: $PRESET"; exit 1 ;;
esac

detect_gpu() {
    if command -v nvidia-smi &>/dev/null && nvidia-smi &>/dev/null 2>&1; then
        echo "nvenc"
    elif command -v vainfo &>/dev/null 2>&1; then
        echo "vaapi"
    else
        echo "cpu"
    fi
}

get_audio_args() {
    case "$1" in
        copy)    echo "-c:a copy" ;;
        aac)     echo "-c:a aac -b:a $AUDIO_BITRATE" ;;
        opus)    echo "-c:a libopus -b:a $AUDIO_BITRATE" ;;
        mp3)     echo "-c:a libmp3lame -b:a $AUDIO_BITRATE" ;;
        flac)    echo "-c:a flac" ;;
        vorbis)  echo "-c:a libvorbis -b:a $AUDIO_BITRATE" ;;
        ac3)     echo "-c:a ac3 -b:a $AUDIO_BITRATE" ;;
        eac3)    echo "-c:a eac3 -b:a $AUDIO_BITRATE" ;;
        pcm_s16le) echo "-c:a pcm_s16le" ;;
        pcm_s24le) echo "-c:a pcm_s24le" ;;
        *)       echo "Códec desconocido: $1"; exit 1 ;;
    esac
}

GPU=$(detect_gpu)

echo "═══════════════════════════════════════"
echo " midu.sh v${VERSION}"
echo "═══════════════════════════════════════"
echo " Entrada:    $INPUT_DIR"
echo " Salida:     $OUTPUT_DIR"
echo " Preset:     $PRESET (CRF $CRF, $SPEED)"
echo " Audio:      $AUDIO_CODEC ($AUDIO_BITRATE)"
echo " Resolución: $RESOLUTION"
[[ -n "$MAX_SIZE" ]] && echo " Tamaño máx: ${MAX_SIZE}MB"
echo " GPU:        $GPU"
echo " Hilos:      $MAX_THREADS"
echo "═══════════════════════════════════════"
echo ""

build_ffmpeg_args() {
    local input="$1"
    local output="$2"
    local remux="$3"
    local gpu="$4"
    local duration="$5"

    local args=(-y -i "$input")

    if [[ "$remux" == "true" ]]; then
        args+=(-c copy -movflags +faststart)
    else
        local res_filter
        res_filter=$(get_resolution_filter "$RESOLUTION")

        local audio_kbps
        audio_kbps=$(echo "$AUDIO_BITRATE" | sed 's/k//')

        if [[ -n "$MAX_SIZE" && -n "$duration" && "$duration" -gt 0 ]]; then
            local total_bits=$((MAX_SIZE * 1024 * 8))
            local total_kbits=$((total_bits / 1000))
            local video_kbits=$((total_kbits - (audio_kbps * duration)))
            local video_bitrate=$((video_kbits / duration))

            if [[ "$video_bitrate" -lt 100 ]]; then
                video_bitrate=100
            fi

            echo "  → Bitrate vídeo calculado: ${video_bitrate}k (para ${MAX_SIZE}MB en ${duration}s)"

            case "$gpu" in
                nvenc) args+=(-c:v h264_nvenc -b:v "${video_bitrate}k" -maxrate "$((video_bitrate * 2))k" -bufsize "${video_bitrate}k" -preset "$SPEED") ;;
                vaapi) args+=(-vaapi_device /dev/dri/renderD128 -c:v h264_vaapi -b:v "${video_bitrate}k" -maxrate "$((video_bitrate * 2))k" -bufsize "${video_bitrate}k") ;;
                *)     args+=(-c:v libx264 -b:v "${video_bitrate}k" -maxrate "$((video_bitrate * 2))k" -bufsize "${video_bitrate}k" -preset "$SPEED") ;;
            esac
        else
            case "$gpu" in
                nvenc) args+=(-c:v h264_nvenc -rc constqp -qp "$CRF" -preset "$SPEED") ;;
                vaapi) args+=(-vaapi_device /dev/dri/renderD128 -c:v h264_vaapi -qp "$CRF") ;;
                *)     args+=(-c:v libx264 -crf "$CRF" -preset "$SPEED") ;;
            esac
        fi

        if [[ -n "$res_filter" ]]; then
            args+=($res_filter)
        fi

        local audio_args
        audio_args=$(get_audio_args "$AUDIO_CODEC")
        args+=($audio_args -movflags +faststart)
    fi

    args+=(-map 0:v:0 -map 0:a:0 -map 0:s?: -map_metadata 0)
    args+=("$output")
    echo "${args[@]}"
}

can_remux() {
    local v_codec a_codec
    v_codec=$(ffprobe -v error -select_streams v:0 -show_entries stream=codec_name -of csv=p=0 "$1" 2>/dev/null)
    a_codec=$(ffprobe -v error -select_streams a:0 -show_entries stream=codec_name -of csv=p=0 "$1" 2>/dev/null)
    [[ "$v_codec" == "h264" && "$a_codec" == "aac" ]]
}

get_duration() {
    ffprobe -v error -show_entries format=duration -of csv=p=0 "$1" 2>/dev/null | cut -d. -f1
}

format_time() {
    printf "%02d:%02d:%02d" $(($1/3600)) $(($1%3600/60)) $(($1%60))
}

convertir_archivo() {
    local file="$1"
    local output_dir="$2"
    local filename
    filename=$(basename "$file")
    filename="${filename%.*}"
    local output_file="$output_dir/$filename.mp4"
    local tmp_file="$output_dir/$filename.mp4.tmp"

    local duration
    duration=$(get_duration "$file")
    local duration_fmt
    duration_fmt=$(format_time "${duration:-0}")

    local remux="false"
    can_remux "$file" && remux="true"

    echo "[$procesados/$total] Procesando: $file (${duration_fmt:-??:??:??}) [remux=$remux]"

    local ffmpeg_args
    IFS=' ' read -r -a ffmpeg_args <<< "$(build_ffmpeg_args "$file" "$tmp_file" "$remux" "$GPU" "$duration")"

    ffmpeg "${ffmpeg_args[@]}" 2>&1 | while IFS= read -r line; do
        if [[ "$line" =~ time=([0-9:]+) ]]; then
            local current="${BASH_REMATCH[1]}"
            local cur_secs=$(echo "$current" | awk -F: '{print ($1*3600)+($2*60)+$3}')
            if [[ -n "$duration" && "$duration" -gt 0 ]]; then
                printf "\r[%3d%%] %s / %s" $((cur_secs * 100 / duration)) "$current" "$duration_fmt"
            fi
        fi
    done
    echo ""

    if [[ $? -ne 0 ]]; then
        echo "[$procesados/$total] ERROR al convertir: $file"
        rm -f "$tmp_file"
        return 1
    fi

    [[ ! -f "$tmp_file" ]] && echo "[$procesados/$total] ERROR: no se creó $tmp_file" && return 1

    local size
    size=$(stat -c%s "$tmp_file" 2>/dev/null || stat -f%z "$tmp_file" 2>/dev/null || echo 0)
    if [[ "$size" -lt 1024 ]]; then
        echo "[$procesados/$total] ERROR: archivo muy pequeño (${size} bytes)"
        rm -f "$tmp_file"
        return 1
    fi

    mv "$tmp_file" "$output_file"
    rm "$file"
    echo "[$procesados/$total] OK: $file → $output_file"
    return 0
}

mkdir -p "$OUTPUT_DIR"

archivos=()
saltados=0
reanudando=0
for ext in "${EXT_ARRAY[@]}"; do
    ext=$(echo "$ext" | xargs)
    while IFS= read -r -d '' file; do
        filename=$(basename "$file")
        filename="${filename%.*}"
        output_file="$OUTPUT_DIR/$filename.mp4"
        tmp_file="$OUTPUT_DIR/$filename.mp4.tmp"

        if [[ -f "$output_file" ]]; then
            out_size=$(stat -c%s "$output_file" 2>/dev/null || stat -f%z "$output_file" 2>/dev/null || echo 0)
            [[ "$out_size" -gt 1024 ]] && ((saltados++)) && continue
        fi

        if [[ -f "$tmp_file" ]]; then
            echo "Reanudando: $file"
            rm -f "$tmp_file"
            ((reanudando++))
        fi

        archivos+=("$file")
    done < <(find "$INPUT_DIR" -maxdepth 2 -type f -iname "*.$ext" -print0)
done

total=${#archivos[@]}

if [[ $total -eq 0 ]]; then
    echo "No hay vídeos nuevos para convertir en $INPUT_DIR"
    [[ $saltados -gt 0 ]] && echo "($saltados ya convertidos)"
    exit 0
fi

echo "Encontrados $total archivos, $saltados ya hechos, $reanudando a reconvertir (hilos: $MAX_THREADS)"
echo ""

procesados=0
fallidos=0
active_threads=()

for file in "${archivos[@]}"; do
    convertir_archivo "$file" "$OUTPUT_DIR" &
    active_threads+=($!)

    while [ "${#active_threads[@]}" -ge "$MAX_THREADS" ]; do
        for i in "${!active_threads[@]}"; do
            if ! kill -0 "${active_threads[i]}" 2>/dev/null; then
                wait "${active_threads[i]}" 2>/dev/null && ((procesados++)) || ((fallidos++))
                unset 'active_threads[i]'
            fi
        done
        active_threads=("${active_threads[@]}")
        sleep 0.5
    done
done

for pid in "${active_threads[@]}"; do
    wait "$pid" 2>/dev/null && ((procesados++)) || ((fallidos++))
done

echo ""
echo "Completado: $procesados OK, $fallidos fallos de $total archivos."
```

### Códecs de audio soportados

| Códec | Uso | Bitrate típico |
|-------|-----|----------------|
| `aac` | Estándar, compatible con todo (default) | 128k-256k |
| `copy` | Sin re-codificar, rápido | — |
| `opus` | Mejor calidad/bitrate, moderno | 96k-192k |
| `mp3` | Máxima compatibilidad | 128k-320k |
| `flac` | Lossless, sin pérdida | — |
| `vorbis` | Open source, buena calidad | 128k-256k |
| `ac3` | Dolby Digital, compatible TV | 192k-640k |
| `eac3` | Dolby Digital Plus | 256k-1024k |
| `pcm_s16le` | Sin compresión, 16-bit | — |
| `pcm_s24le` | Sin compresión, 24-bit | — |

## 3. Estructura de carpetas

```
test_videos/
├── docker-compose.yml
├── midu.sh
├── test/              ← meter aquí los vídeos a convertir
└── optimizados/       ← se crea automáticamente con los mp4
```

## 4. Pasos para ejecutarlo en WSL

Abre tu terminal de WSL (Ubuntu) y sigue estos pasos:

1. **Asegurar Docker Desktop:** En Windows. Asegúrate de que Docker Desktop está abierto y que en sus ajustes (`Settings -> Resources -> WSL Integration`) tienes activada la casilla de tu distribución de WSL.

2. **Lanzar el contenedor:** En WSL, ve a la carpeta donde guardaste los archivos y arráncalo en modo interactivo:

    ```sh
    docker compose run --rm downloader
    ```

3. **Descargar vídeo:** Dentro de Alpine, una vez veas el prompt `/ #`, pega el comando de descarga. Gracias a ffmpeg, bajará la máxima calidad disponible y unirá el audio y vídeo automáticamente:

    ```sh
    yt-dlp -f "bv+ba/b" --merge-output-format mp4 -P /test_videos/test "AQUÍ_TU_URL"
    ```

Al terminar, el archivo aparecerá en la carpeta `test/`.

**Importante:** El script `midu.sh` se ejecuta automáticamente al iniciar el contenedor. Esto significa que la primera vez no encontrará vídeos para convertir (porque aún no has descargado nada). Después de descargar vídeos con `yt-dlp`, ejecuta la conversión manualmente dentro del contenedor:

```sh
bash /test_videos/midu.sh
```

O reinicia el contenedor para que se ejecute de nuevo automáticamente.

Cuando acabes, simplemente escribe `exit` en la terminal del contenedor.

> **Nota:** Si usas una versión antigua de Docker, puede que necesites `docker-compose` (con guion) en vez de `docker compose` (con espacio).

## 5. Ejecutar directamente en Ubuntu (sin Docker)

Si no quieres Docker, puedes ejecutar el script directamente en Ubuntu. Solo necesitas instalar las dependencias:

```sh
sudo apt update && sudo apt install ffmpeg
```

Para instalar yt-dlp:

```sh
sudo curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o /usr/local/bin/yt-dlp && sudo chmod a+rx /usr/local/bin/yt-dlp
```

Ajusta las rutas en `midu.sh` para que apunten a carpetas locales en vez de `/test_videos/`:

```bash
input_dir="$HOME/Videos/test"
output_dir="$HOME/Videos/optimizados"
```

Añade el shebang al inicio del script (sin Docker es necesario para ejecución directa):

```bash
#!/bin/bash
```

Hazlo ejecutable y ejecútalo:

```sh
chmod +x midu.sh
./midu.sh
```

## 6. Ejecutar con Docker en Ubuntu (sin WSL)

Si usas Ubuntu directamente (sin WSL ni Windows), el proceso es similar pero las rutas cambian. Primero instala Docker:

```sh
sudo apt update && sudo apt install docker.io docker-compose-v2
sudo usermod -aG docker $USER
```

Cierra y vuelve a abrir la terminal para que el cambio de grupo surta efecto. Ajusta las rutas en `docker-compose.yml` para que apunten a carpetas de tu home de Linux:

```yaml
services:
  downloader:
    image: alpine:latest
    container_name: yt_ffmpeg_downloader
    volumes:
      - /home/TuUsuario/Videos/test_videos:/test_videos
    working_dir: /test_videos
    user: root
    tty: true          # Equivalente a -t en docker run
    stdin_open: true   # Equivalente a -i en docker run
    command: >-
      sh -c "apk update &&
      apk add --no-cache ffmpeg python3 curl bash su-exec &&
      adduser -D -u $(id -u) tu_usuario &&
      curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o /usr/local/bin/yt-dlp &&
      chmod a+rx /usr/local/bin/yt-dlp &&
      su-exec tu_usuario bash /test_videos/midu.sh &&
      su-exec tu_usuario sh"
```

> **Sobre los permisos:** Igual que en la sección de WSL, usamos `user: root` + `su-exec` para evitar que los archivos se creen como `root`. Cambia `tu_usuario` por el nombre de tu usuario en Linux (`whoami`).

Lanza el contenedor:

```sh
docker compose run --rm downloader
```

> **Nota:** Si `docker compose` no funciona, prueba con `docker-compose` (con guion).

## 7. Desinstalar ffmpeg y yt-dlp locales

Si instalaste ffmpeg y yt-dlp en tu sistema y ahora prefieres usarlos solo desde Docker:

```sh
sudo apt remove --purge ffmpeg
sudo rm /usr/local/bin/yt-dlp
```

Para limpiar las dependencias que ya no se usan:

```sh
sudo apt autoremove
```

Esto no afecta al contenedor Docker, ya que Alpine instala sus propias copias de ffmpeg y yt-dlp al arrancar.

## 8. Problemas que encontré y cómo los resolví

### Saltos de línea CRLF de Windows

**Errores:**

```
bash: line 7: mkdi: command not found
bash: line 29: syntax error near unexpected token `do'
bash: line 29: `fo ext in "${extensiones[@]}"; do'
```

`mkdir` aparecía como `mkdi` y `for` como `fo`.

**Causa:** El `docker-compose.yml` tenía saltos de línea Windows (CRLF = `\r\n`). El `\r` se incluía en el contenido del script que bash recibía, y el carriage return movía el cursor al inicio de la línea, haciendo que bash viera nombres de comando truncados.

**Solución:** Reescribir el docker-compose.yml con saltos de línea LF (Unix). Si usas VS Code, haz clic en "CRLF" en la barra de estado inferior y cambia a "LF".

### Shebang innecesario (solo en Docker)

**Error:** `bash: line 1: #!/bin/bash: No such file or directory`

El script tenía `#!/bin/bash` pero Docker ejecutaba el contenido con `bash -c`. El shebang solo sirve cuando ejecutas el script directamente (`./midu.sh`).

**Solución en Docker:** Eliminar la línea `#!/bin/bash` del script, ya que bash se invoca explícitamente.

**En Ubuntu sin Docker:** El shebang es necesario. Si ejecutas `./midu.sh`, bash necesita saber qué intérprete usar.

### Regex innecesaria con sed

El script original usaba `sed` para quitar la extensión del archivo:

```bash
local filename=$(basename "$file" | sed 's/\.[^.]*$//')
```

**Solución:** Reemplazar con parameter expansion de bash, más limpio y sin dependencia de `sed`:

```bash
local filename=$(basename "$file")
filename="${filename%.*}"
```

### Archivos creados como root en los volúmenes

**Problema:** Al ejecutar el contenedor, los archivos descargados y convertidos quedan como `root:root` en vez de tu usuario normal:

```sh
$ ls -la /home/tu_usuario/Videos/test_videos/test/
-rw-r--r-- 1 root root 12345678 video.mp4  # ← incorrecto
```

Esto pasa porque Alpine ejecuta todos los comandos como `root` por defecto.

**Solución:** Usar `su-exec` para ejecutar el script y la shell como tu usuario, manteniendo `user: root` solo para la instalación de paquetes (que necesita permisos de administrador):

```yaml
user: root
command: >-
  sh -c "apk update &&
  apk add --no-cache ffmpeg python3 curl bash su-exec &&
  adduser -D -u 1000 tu_usuario &&
  curl -L ... -o /usr/local/bin/yt-dlp &&
  chmod a+rx /usr/local/bin/yt-dlp &&
  su-exec tu_usuario bash /test_videos/midu.sh &&
  su-exec tu_usuario sh"
```

- `adduser -D -u 1000 tu_usuario` — crea el usuario dentro del contenedor con el mismo UID que tu usuario del host. Cambia `tu_usuario` por tu nombre de usuario real (`whoami`).
- `su-exec tu_usuario bash midu.sh` — ejecuta el script como tu usuario en vez de root.

Si ya tienes archivos creados como root, corrígelos con:

```sh
sudo chown -R tu_usuario:tu_usuario /home/tu_usuario/Videos/test_videos
```

## 9. yt-dlp: Comandos y opciones

### Descarga básica

```sh
# Descargar vídeo con mejor calidad disponible
yt-dlp "URL"

# Listar todos los formatos disponibles
yt-dlp -F "URL"

# Descargar con mejor vídeo + mejor audio (requiere ffmpeg para unir)
yt-dlp -f "bv+ba/b" "URL"

# Forzar merge a mp4
yt-dlp -f "bv+ba/b" --merge-output-format mp4 "URL"

# Simular sin descargar (mostraría qué haría)
yt-dlp --simulate "URL"
```

### Selección de formato y calidad

```sh
# Máxima calidad disponible
yt-dlp -f "bestvideo+bestaudio/best" "URL"

# Cap a 1080p máximo
yt-dlp -f "bestvideo[height<=1080]+bestaudio/best" "URL"

# Forzar 4K si está disponible
yt-dlp -f "bestvideo[height>=2160]+bestaudio/best" "URL"

# Preferir H.264/MP4 (máxima compatibilidad)
yt-dlp -f "bestvideo[ext=mp4]+bestaudio[ext=m4a]/best[ext=mp4]/best" "URL"

# Preferir VP9 (mejor calidad en mismo bitrate)
yt-dlp -f "bestvideo[vcodec^=vp9]+bestaudio/best" "URL"

# Calidad específica (ejemplo: formato 22 = 720p mp4)
yt-dlp -f 22 "URL"
```

### Extraer solo audio

```sh
# Extraer audio y convertir a mp3
yt-dlp -x --audio-format mp3 "URL"

# Extraer audio a calidad máxima (0 = mejor)
yt-dlp -x --audio-format mp3 --audio-quality 0 "URL"

# Extraer audio a 128k
yt-dlp -x --audio-format mp3 --audio-quality 128k "URL"

# Extraer audio como opus (menor tamaño, mejor calidad)
yt-dlp -x --audio-format opus "URL"

# Extraer audio como aac
yt-dlp -x --audio-format m4a "URL"

# Extraer audio sin re-codificar (si ya es mp3, solo extraer)
yt-dlp -x --audio-format mp3 --audio-quality -1 "URL"
```

### Playlists

```sh
# Descargar playlist completa
yt-dlp "URL_PLAYLIST"

# Descargar solo los primeros 5 vídeos
yt-dlp --playlist-items 1-5 "URL_PLAYLIST"

# Descargar del vídeo 3 al 10
yt-dlp --playlist-items 3-10 "URL_PLAYLIST"

# Solo vídeos nuevos (guardar en archivo de archive)
yt-dlp --download-archive watched.txt "URL_PLAYLIST"

# Descargar solo vídeos de los últimos 30 días
yt-dlp --dateafter today-30days "URL_PLAYLIST"

# No descargar, solo listar títulos
yt-dlp --flat-playlist --print title "URL_PLAYLIST"

# Mantener orden de la playlist
yt-dlp --yes-playlist "URL_PLAYLIST"
```

### Subtítulos

```sh
# Descargar subtítulos automáticos
yt-dlp --write-auto-subs "URL"

# Descargar subtítulos en español e inglés
yt-dlp --write-subs --sub-lang es,en "URL"

# Solo subtítulos (sin vídeo)
yt-dlp --write-subs --sub-lang es --skip-download "URL"

# Formato específico de subtítulos
yt-dlp --write-subs --sub-format srt "URL"

# Incrustar subtítulos en el vídeo (requiere ffmpeg)
yt-dlp --embed-subs --sub-lang es "URL"
```

### Metadatos y thumbnails

```sh
# Incrustar thumbnail en el archivo
yt-dlp --embed-thumbnail "URL"

# Incrustar metadata (título, autor, etc.)
yt-dlp --embed-metadata "URL"

# Incrustar todo (thumbnail + metadata + subtítulos)
yt-dlp --embed-thumbnail --embed-metadata --embed-subs "URL"

# Guardar thumbnail como archivo separado
yt-dlp --write-thumbnail "URL"

# Guardar descripción del vídeo
yt-dlp --write-description "URL"

# Guardar información en formato JSON
yt-dlp -j "URL" > info.json
```

### Cookies y autenticación

```sh
# Usar cookies del navegador (para vídeos privados o con restricción de edad)
yt-dlp --cookies-from-browser chrome "URL"

# Usar cookies de Firefox
yt-dlp --cookies-from-browser firefox "URL"

# Usar archivo de cookies (Netscape format)
yt-dlp --cookies cookies.txt "URL"

# Login con usuario y contraseña (no recomendado, usar cookies)
yt-dlp --username usuario --password contraseña "URL"
```

### Rendimiento y red

```sh
# Fragmentos paralelos (más rápido en conexiones buenas)
yt-dlp -N 4 "URL"

# Limitar velocidad de descarga
yt-dlp --limit-rate 5M "URL"

# Limitar a 500KB/s
yt-dlp --limit-rate 500K "URL"

# Reintentos en fallos
yt-dlp --retries 10 --fragment-retries 10 "URL"

# Intervalo aleatorio entre descargas (evitar bans)
yt-dlp --sleep-interval 5 --max-sleep-interval 10 "URL"

# Usar proxy
yt-dlp --proxy "socks5://127.0.0.1:1080" "URL"

# Forzar IPv4
yt-dlp -4 "URL"

# Forzar IPv6
yt-dlp -6 "URL"
```

### Plantillas de salida

```sh
# Estructura por canal
yt-dlp -o "%(channel)s/%(title)s.%(ext)s" "URL"

# Con fecha
yt-dlp -o "%(channel)s/%(upload_date>%Y-%m-%d)s-%(title)s.%(ext)s" "URL"

# Con índice de playlist
yt-dlp -o "%(playlist_index)02d-%(title)s.%(ext)s" "URL_PLAYLIST"

# Variables disponibles:
# %(title)s     → Título del vídeo
# %(ext)s       → Extensión del archivo
# %(id)s        → ID del vídeo
# %(upload_date)s → Fecha (YYYYMMDD)
# %(channel)s   → Nombre del canal
# %(duration)s  → Duración en segundos
# %(view_count)s → Número de vistas
```

### Post-procesamiento

```sh
# Re-codificar a mp4 después de descargar
yt-dlp --recode-video mp4 "URL"

# Ejecutar comando post-descarga
yt-dlp --exec "echo Descargado: {}" "URL"

# Abrir con reproductor después de descargar
yt-dlp --exec "xdg-open" "URL"

# No re-codificar, solo cambiar contenedor
yt-dlp --merge-output-format mp4 "URL"
```

### Grabación de livestream

```sh
# Grabar livestream desde el principio
yt-dlp --live-from-start "URL"

# Grabar fragmento específico de un directo
yt-dlp --live-from-start --live-fragments-start 100 "URL"
```

### Opciones útiles

```sh
# Ignorar errores y continuar
yt-dlp --ignore-errors "URL"

# Forzar re-descarga aunque exista
yt-dlp -f "best" --force-overwrites "URL"

# No sobreescribir archivos existentes
yt-dlp --no-overwrites "URL"

# Ver información del vídeo sin descargar
yt-dlp --dump-json "URL"

# Ver formatos disponibles en tabla
yt-dlp -F "URL"

# Verificar versión
yt-dlp --version

# Actualizar yt-dlp
yt-dlp -U
```

### Archivo de configuración

Crea `~/.config/yt-dlp/config`:

```
--format bv*+ba
--merge-output-format mp4
--embed-thumbnail
--embed-metadata
--write-subs
--sub-lang es,en
--output ~/Videos/%(channel)s/%(title)s.%(ext)s
```

Se ejecuta automáticamente al usar yt-dlp.

## 10. ffmpeg: Comandos y opciones

### Conversión de formatos

```sh
# Convertir a mp4
ffmpeg -i input.avi output.mp4

# Convertir a mkv
ffmpeg -i input.mp4 output.mkv

# Convertir a webm (VP9)
ffmpeg -i input.mp4 -c:v libvpx-vp9 -crf 30 output.webm

# Convertir a mov (Apple)
ffmpeg -i input.mp4 output.mov

# Sin re-codificar (solo cambiar contenedor)
ffmpeg -i input.mkv -c copy output.mp4
```

### Extracción de audio

```sh
# Extraer audio sin re-codificar
ffmpeg -i video.mp4 -vn -c:a copy audio.aac

# Extraer y convertir a mp3
ffmpeg -i video.mp4 -vn -c:a libmp3lame -b:a 192k audio.mp3

# Extraer a wav (sin compresión)
ffmpeg -i video.mp4 -vn -c:a pcm_s16le audio.wav

# Extraer a flac (lossless)
ffmpeg -i video.mp4 -vn -c:a flac audio.flac

# Extraer solo un canal (mono)
ffmpeg -i video.mp4 -vn -ac 1 audio.mp3

# Extraer a 22050 Hz (para voz/podcasts)
ffmpeg -i video.mp4 -vn -ar 22050 audio.mp3
```

### Corte de vídeo

```sh
# Cortar por tiempo (rápido, sin re-codificar)
ffmpeg -i input.mp4 -ss 00:01:00 -to 00:02:00 -c copy output.mp4

# Cortar con re-codificación (preciso)
ffmpeg -i input.mp4 -ss 00:01:00 -to 00:02:00 -c:v libx264 -c:a aac output.mp4

# Cortar los primeros 30 segundos
ffmpeg -i input.mp4 -t 00:00:30 -c copy output.mp4

# Cortar desde el segundo 60 hasta el final
ffmpeg -i input.mp4 -ss 00:01:00 -c copy output.mp4

# Cortar por frames (ejemplo: del frame 100 al 500)
ffmpeg -i input.mp4 -vf "select=between(n\,100\,500)" -c:v libx264 output.mp4
```

### Concatenación de vídeos

```sh
# Método 1: filter_complex (mismos formatos)
ffmpeg -i video1.mp4 -i video2.mp4 -filter_complex "[0:v][0:a][1:v][1:a]concat=n=2:v=1:a=1[outv][outa]" -map "[outv]" -map "[outa]" output.mp4

# Método 2: archivo de lista (para muchos archivos)
# Crear archivo lista.txt:
# file 'video1.mp4'
# file 'video2.mp4'
# file 'video3.mp4'
ffmpeg -f concat -safe 0 -i lista.txt -c copy output.mp4

# Concatenar 3 vídeos
ffmpeg -i v1.mp4 -i v2.mp4 -i v3.mp4 -filter_complex "[0:v][0:a][1:v][1:a][2:v][2:a]concat=n=3:v=1:a=1" output.mp4
```

### Redimensionado y escalado

```sh
# Escalar a 1280x720
ffmpeg -i input.mp4 -vf scale=1280:720 output.mp4

# Manteniendo aspect ratio (con padding negro)
ffmpeg -i input.mp4 -vf "scale=1280:720:force_original_aspect_ratio=decrease,pad=1280:720:(ow-iw)/2:(oh-ih)/2" output.mp4

# Escalar al 50%
ffmpeg -i input.mp4 -vf scale=iw/2:ih/2 output.mp4

# Escalar por ancho (alto automático)
ffmpeg -i input.mp4 -vf "scale=1280:-2" output.mp4

# Escalar por alto (ancho automático)
ffmpeg -i input.mp4 -vf "scale=-2:720" output.mp4

# Cropping (recortar)
ffmpeg -i input.mp4 -vf "crop=640:480:100:50" output.mp4
# crop=ancho:alto:x:y
```

### Video a GIF

```sh
# GIF básico
ffmpeg -i input.mp4 output.gif

# GIF con calidad (paleta optimizada)
ffmpeg -i input.mp4 -vf "fps=10,scale=320:-1:flags=lanczos,split[s0][s1];[s0]palettegen[p];[s1][p]paletteuse" output.gif

# GIF de un segmento específico
ffmpeg -i input.mp4 -ss 00:01:00 -t 5 -vf "fps=15,scale=480:-1:flags=lanczos,split[s0][s1];[s0]palettegen[p];[s1][p]paletteuse" output.gif
```

### Marca de agua

```sh
# Imagen como marca de agua (esquina inferior derecha)
ffmpeg -i video.mp4 -i logo.png -filter_complex "overlay=W-w-10:H-h-10" output.mp4

# Imagen en esquina superior izquierda
ffmpeg -i video.mp4 -i logo.png -filter_complex "overlay=10:10" output.mp4

# Texto como marca de agua
ffmpeg -i video.mp4 -vf "drawtext=text='Mi Marca':fontsize=24:fontcolor=white:x=10:y=10" output.mp4

# Texto con sombra
ffmpeg -i video.mp4 -vf "drawtext=text='© 2026':fontsize=20:fontcolor=white:borderw=2:bordercolor=black:x=W-tw-10:y=H-th-10" output.mp4

# Marca de agua semi-transparente
ffmpeg -i video.mp4 -i logo.png -filter_complex "format=rgba,colorchannelmixer=aa=0.3[wm];[0:v][wm]overlay=W-w-10:H-h-10" output.mp4
```

### Filtros de vídeo

```sh
# Ajustar brillo (0.0 a 2.0, 1.0 = normal)
ffmpeg -i input.mp4 -vf "eq=brightness=0.1" output.mp4

# Ajustar contraste (0.0 a 2.0, 1.0 = normal)
ffmpeg -i input.mp4 -vf "eq=contrast=1.2" output.mp4

# Ajustar saturación
ffmpeg -i input.mp4 -vf "eq=saturation=1.5" output.mp4

# Filtro de nitidez
ffmpeg -i input.mp4 -vf "unsharp=5:5:1.0" output.mp4

# Filtro de desenfoque
ffmpeg -i input.mp4 -vf "boxblur=5:5" output.mp4

# Voltear horizontalmente
ffmpeg -i input.mp4 -vf "hflip" output.mp4

# Voltear verticalmente
ffmpeg -i input.mp4 -vf "vflip" output.mp4

# Rotar 90 grados
ffmpeg -i input.mp4 -vf "transpose=1" output.mp4

# Rotar 180 grados
ffmpeg -i input.mp4 -vf "transpose=2,transpose=2" output.mp4
```

### Subtítulos

```sh
# Incrustar subtítulos SRT en el vídeo
ffmpeg -i video.mp4 -vf "subtitles=subtitles.srt" output.mp4

# Incrustar con estilo personalizado
ffmpeg -i video.mp4 -vf "subtitles=subtitles.srt:force_style='FontSize=24,PrimaryColour=&H00FFFFFF'" output.mp4

# Cambiar编码 de subtítulos
ffmpeg -i video.mp4 -c:s srt output.mp4

# Copiar subtítulos sin re-codificar
ffmpeg -i video.mkv -c:s copy output.mp4
```

### Normalización de audio

```sh
# Normalización simple
ffmpeg -i input.mp4 -af "loudnorm" output.mp4

# Normalización dinámica
ffmpeg -i input.mp4 -af "dynaudnorm" output.mp4

# Ajustar volumen (2x más fuerte)
ffmpeg -i input.mp4 -af "volume=2.0" output.mp4

# Ajustar volumen (50% más bajo)
ffmpeg -i input.mp4 -af "volume=0.5" output.mp4

# Ecualización (ejemplo: realzar graves)
ffmpeg -i input.mp4 -af "equalizer=f=100:t=q:w=1:g=5" output.mp4
```

### Dos pasadas (two-pass) para mejor calidad

```sh
# Pasada 1: análisis
ffmpeg -i input.mp4 -c:v libx264 -b:v 5M -pass 1 -f null /dev/null

# Pasada 2: codificación
ffmpeg -i input.mp4 -c:v libx264 -b:v 5M -pass 2 output.mp4

# Para H.265/HEVC
ffmpeg -i input.mp4 -c:v libx265 -b:v 3M -pass 1 -f null /dev/null
ffmpeg -i input.mp4 -c:v libx265 -b:v 3M -pass 2 output.mp4
```

### Streaming HLS

```sh
# Preparar para streaming HLS
ffmpeg -i input.mp4 -c:v libx264 -c:a aac -f hls -hls_time 4 -hls_playlist_type vod output.m3u8

# Multi-bitrate (adaptativo)
ffmpeg -i input.mp4 \
  -map 0 -b:v:0 500k -s:v:0 640x360 -c:v libx264 \
  -b:v:1 1000k -s:v:1 1280:720 -c:v libx264 \
  -b:v:2 2000k -s:v:2 1920:1080 -c:v libx264 \
  -hls_time 4 -hls_playlist_type vod \
  -var_stream_map "v:0,a:0 v:1,a:0 v:2,a:0" \
  stream_%v.m3u8
```

### Grabación de pantalla y webcam

```sh
# Grabar pantalla (Linux, X11)
ffmpeg -f x11grab -framerate 30 -video_size 1920x1080 -i :0.0 -c:v libx264 output.mp4

# Grabar pantalla con audio
ffmpeg -f x11grab -framerate 30 -video_size 1920x1080 -i :0.0 -f pulse -i default -c:v libx264 -c:a aac output.mp4

# Grabar webcam
ffmpeg -f v4l2 -framerate 30 -video_size 640x480 -i /dev/video0 -c:v libx264 output.mp4

# Grabar区域 específica de la pantalla
ffmpeg -f x11grab -framerate 30 -video_size 640x480 -i :0.0+100,200 output.mp4
```

### Procesamiento por lotes

```sh
# Convertir todos los avi a mp4
for file in *.avi; do ffmpeg -i "$file" "${file%.avi}.mp4"; done

# Convertir todos los mkv a mp4 sin re-codificar
for file in *.mkv; do ffmpeg -i "$file" -c copy "${file%.mkv}.mp4"; done

# Redimensionar todos los vídeos a 720p
for file in *.mp4; do ffmpeg -i "$file" -vf scale=1280:720 "${file%.mp4}_720p.mp4"; done

# Extraer audio de todos los vídeos
for file in *.mp4; do ffmpeg -i "$file" -vn -c:a libmp3lame "${file%.mp4}.mp3"; done
```

### Opciones útiles

```sh
# Sobreescribir sin preguntar
ffmpeg -y -i input.mp4 output.mp4

# No sobreescribir
ffmpeg -n -i input.mp4 output.mp4

# Ocultar banner de información
ffmpeg -hide_banner -i input.mp4

# Ver información del archivo
ffprobe -v error -show_format -show_streams input.mp4

# Ver duración
ffprobe -v error -show_entries format=duration -of csv=p=0 input.mp4

# Ver codec de vídeo
ffprobe -v error -select_streams v:0 -show_entries stream=codec_name -of csv=p=0 input.mp4

# Ver codec de audio
ffprobe -v error -select_streams a:0 -show_entries stream=codec_name -of csv=p=0 input.mp4
```

## 11. Combinados: yt-dlp + ffmpeg

### Descargar y reconvertir

```sh
# Descargar y convertir a mp4 con ffmpeg
yt-dlp -f "bv+ba/b" "URL" --exec "ffmpeg -i {} -c:v libx264 -c:a aac {}.mp4"

# Descargar y redimensionar a 720p
yt-dlp -f "bv+ba/b" "URL" --exec "ffmpeg -i {} -vf scale=1280:720 -c:v libx264 -c:a aac {}_720p.mp4"

# Descargar y comprimir a tamaño específico
yt-dlp -f "bv+ba/b" "URL" --exec "ffmpeg -i {} -c:v libx264 -b:v 1M -c:a aac {}_compressed.mp4"
```

### Descargar solo audio y convertir

```sh
# Descargar audio directamente con yt-dlp
yt-dlp -x --audio-format mp3 "URL"

# Descargar audio y convertir con ffmpeg (más control)
yt-dlp -x --audio-format wav "URL" --exec "ffmpeg -i {} -c:a libmp3lame -b:a 320k {}.mp3"

# Descargar playlist como mp3
yt-dlp -x --audio-format mp3 --yes-playlist "URL_PLAYLIST"
```

### Pipeline completo: descargar → comprimir → organizar

```sh
#!/bin/bash
# Script para descargar y comprimir vídeos

URL="$1"
CARPETA="$HOME/Videos/$(date +%Y-%m-%d)"
mkdir -p "$CARPETA"

# Descargar
yt-dlp -f "bv+ba/b" --merge-output-format mp4 -o "$CARPETA/%(title)s.%(ext)s" "URL"

# Comprimir todos los vídeos nuevos
for file in "$CARPETA"/*.mp4; do
    ffmpeg -i "$file" -c:v libx264 -crf 28 -preset fast -c:a aac -b:a 128k "${file%.mp4}_compressed.mp4"
    rm "$file"
done
```

### Workflow de lotes: descargar playlist + comprimir

```sh
#!/bin/bash
# Descargar playlist y comprimir cada vídeo

PLAYLIST_URL="$1"
OUTPUT_DIR="$HOME/Videos/comprimidos"
mkdir -p "$OUTPUT_DIR"

# Descargar playlist
yt-dlp -f "bv+ba/b" --merge-output-format mp4 \
  -o "$OUTPUT_DIR/%(playlist_index)02d-%(title)s.%(ext)s" \
  --yes-playlist "$PLAYLIST_URL"

# Comprimir cada vídeo
for file in "$OUTPUT_DIR"/*.mp4; do
    echo "Comprimiendo: $file"
    ffmpeg -i "$file" \
      -c:v libx264 -crf 28 -preset fast \
      -c:a aac -b:a 128k \
      -movflags +faststart \
      "${file%.mp4}_small.mp4"
    rm "$file"
done

echo "¡Listo! Vídeos comprimidos en $OUTPUT_DIR"
```

### Descargar segmento específico y comprimir

```sh
# Descargar solo un trozo de un vídeo
yt-dlp --external-downloader ffmpeg \
  --external-downloader-args "ffmpeg_i:-ss 00:10:00 -to 00:20:00" \
  -f "bv+ba/b" "URL"

# Descargar y comprimir ese segmento
yt-dlp --external-downloader ffmpeg \
  --external-downloader-args "ffmpeg_i:-ss 00:10:00 -to 00:20:00" \
  -f "bv+ba/b" "URL" \
  --exec "ffmpeg -i {} -c:v libx264 -crf 28 -c:a aac {}.mp4"
```

### Descargar canal completo y organizar por carpeta

```sh
# Descargar canal organizado por carpetas
yt-dlp -o "%(channel)s/%(playlist_title)s/%(title)s.%(ext)s" "URL_CANAL"

# Descargar canal con límite de calidad
yt-dlp -f "bestvideo[height<=1080]+bestaudio/best" \
  -o "%(channel)s/%(title)s.%(ext)s" "URL_CANAL"
```

## 12. yt-dlp: Opciones avanzadas

### Grabación de canales completos

```sh
# Descargar canal completo (todas las playlists)
yt-dlp --yes-playlist "URL_CANAL/videos"

# Descargar canal con límite de calidad
yt-dlp -f "bestvideo[height<=1080]+bestaudio/best" \
  --yes-playlist "URL_CANAL/videos"

# Descargar canal y organizar por fecha
yt-dlp -o "%(channel)s/%(upload_date>%Y-%m-%d)s-%(title)s.%(ext)s" \
  --yes-playlist "URL_CANAL/videos"

# Descargar solo los últimos 10 vídeos
yt-dlp --playlist-items 1-10 "URL_CANAL/videos"

# Descargar canal y comprimir cada vídeo
yt-dlp --yes-playlist -f "bv+ba/b" "URL_CANAL/videos" \
  --exec "ffmpeg -i {} -c:v libx264 -crf 28 -c:a aac {}_small.mp4"
```

### Descarga con proxy/VPN

```sh
# Usar proxy SOCKS5
yt-dlp --proxy "socks5://127.0.0.1:1080" "URL"

# Usar proxy HTTP
yt-dlp --proxy "http://127.0.0.1:8080" "URL"

# Usar proxy con autenticación
yt-dlp --proxy "socks5://usuario:password@127.0.0.1:1080" "URL"

# Forzar IPv4 (si IPv6 tiene problemas)
yt-dlp -4 "URL"

# Forzar IPv6
yt-dlp -6 "URL"

# Usar Tor
yt-dlp --proxy "socks5://127.0.0.1:9050" "URL"
```

### Bypass a restricciones

```sh
# Saltar restricción de edad
yt-dlp --age-limit 100 "URL"

# Usar cookies de navegador autenticado
yt-dlp --cookies-from-browser chrome "URL"

# Forzar cliente web (para YouTube en 2026)
yt-dlp --extractor-args "youtube:player_client=web" "URL"

# Usar cliente Android
yt-dlp --extractor-args "youtube:player_client=android" "URL"

# Usar cliente iOS
yt-dlp --extractor-args "youtube:player_client=ios" "URL"
```

### Errores comunes y soluciones

| Error | Causa | Solución |
|-------|-------|----------|
| `ERROR: Unable to extract` | YouTube cambió su estructura | `yt-dlp -U` (actualizar) |
| `HTTP Error 403` | Bloqueo de rate limiting | `--sleep-interval 5` |
| `ERROR: Sign in to confirm` | Necesita cookies | `--cookies-from-browser chrome` |
| `ERROR: PoToken` | Token de prueba falla | `--extractor-args "youtube:player_client=web"` |
| `ERROR: File already exists` | Archivo existente | `--no-overwrites` |
| `ERROR: Network unreachable` | Problema de red | `--proxy` o `-4` |
| `ERROR: Fragment download failed` | Fragmento corrupto | `--fragment-retries 10` |
| `ERROR: Requested format not available` | Formato no existe | `yt-dlp -F "URL"` (listar formatos) |

### Solución a PoToken (YouTube 2026)

YouTube requiere un Proof-of-Origin Token en 2026. Soluciones:

```sh
# Opción 1: Usar cookies de navegador
yt-dlp --cookies-from-browser chrome "URL"

# Opción 2: Forzar extractor específico
yt-dlp --extractor-args "youtube:player_client=web" "URL"

# Opción 3: Usar bgutil-ytdlp-pot-provider (recomendado)
# Instalar: pip install bgutil-ytdlp-pot-provider
# Luego usar normalmente
yt-dlp "URL"
```

### Integración con otros herramientas

```sh
# Abrir en VLC después de descargar
yt-dlp --exec "vlc {}" "URL"

# Mover a carpeta de Plex
yt-dlp -o "%(channel)s/%(title)s.%(ext)s" "URL" \
  --exec "mv {} /srv/media/videos/"

# Enviar notificación cuando termine
yt-dlp "URL" --exec "notify-send 'Descarga completada' '{}'"

# Agregar a cola de JDownloader (no recomendado, usar yt-dlp directamente)
yt-dlp --write-info-json "URL"

# Crear symlink para organización
yt-dlp -o "%(upload_date>%Y-%m-%d)s-%(title)s.%(ext)s" "URL" \
  --exec "ln -s {} /srv/videos/nuevos/"
```

## 13. ffmpeg: Filtros y efectos avanzados

### Filtros de imagen

```sh
# Desentrelazar (deinterlace)
ffmpeg -i input.mp4 -vf bwdif output.mp4

# Reducir ruido
ffmpeg -i input.mp4 -vf "nlmeans=s=3:p=7:r=3" output.mp4

# Reducir ruido (más rápido)
ffmpeg -i input.mp4 -vf "hqdn3d=4:3:6:4.5" output.mp4

# Nitidez avanzada
ffmpeg -i input.mp4 -vf "unsharp=3:3:1.5:3:3:0.0" output.mp4

# Viñeta (oscurecer bordes)
ffmpeg -i input.mp4 -vf "vignette=PI/4" output.mp4

# Corrección de lente
ffmpeg -i input.mp4 -vf "lenscorrection=cx=0.5:cy=0.5:k1=0.1" output.mp4
```

### Efectos de movimiento

```sh
# Cámara lenta (50% velocidad)
ffmpeg -i input.mp4 -vf "setpts=2.0*PTS" -af "atempo=0.5" output.mp4

# Cámara lenta 4x
ffmpeg -i input.mp4 -vf "setpts=4.0*PTS" -af "atempo=0.5,atempo=0.5" output.mp4

# Acelerar 2x
ffmpeg -i input.mp4 -vf "setpts=0.5*PTS" -af "atempo=2.0" output.mp4

# Acelerar 4x
ffmpeg -i input.mp4 -vf "setpts=0.25*PTS" -af "atempo=2.0,atempo=2.0" output.mp4

# Reverse (al revés)
ffmpeg -i input.mp4 -vf reverse -af areverse output.mp4

# Boomerang (adelante y atrás)
ffmpeg -i input.mp4 -vf "reverse,split[out0][out1];[out0]reverse[reversed];[out1][reversed]concat=n=2:v=1:a=0" output.mp4
```

### Picture-in-Picture (PiP)

```sh
# Vídeo pequeño en esquina superior derecha
ffmpeg -i main.mp4 -i pip.mp4 \
  -filter_complex "[1:v]scale=320:-1[pip];[0:v][pip]overlay=W-w-10:10" \
  output.mp4

# Vídeo pequeño en esquina inferior izquierda
ffmpeg -i main.mp4 -i pip.mp4 \
  -filter_complex "[1:v]scale=320:-1[pip];[0:v][pip]overlay=10:H-h-10" \
  output.mp4

# PiP con borde
ffmpeg -i main.mp4 -i pip.mp4 \
  -filter_complex "[1:v]scale=320:-1,pad=324:ih+4:2:2:white[pip];[0:v][pip]overlay=W-w-10:10" \
  output.mp4

# PiP animado (aparece después de 5 segundos)
ffmpeg -i main.mp4 -i pip.mp4 \
  -filter_complex "[1:v]scale=320:-1[pip];[0:v][pip]overlay=enable='gte(t,5)':W-w-10:10" \
  output.mp4
```

### Transiciones

```sh
# Fade out al final
ffmpeg -i input.mp4 -vf "fade=t=out:st=55:d=5" -af "afade=t=out:st=55:d=5" output.mp4

# Fade in al inicio
ffmpeg -i input.mp4 -vf "fade=t=in:st=0:d=5" -af "afade=t=in:st=0:d=5" output.mp4

# Fade in + fade out
ffmpeg -i input.mp4 -vf "fade=t=in:st=0:d=3,fade=t=out:st=57:d=3" output.mp4

# Dissolve entre 2 vídeos
ffmpeg -i v1.mp4 -i v2.mp4 -filter_complex \
  "[0:v]fade=t=out:st=4:d=1[v0];[1:v]fade=t=in:st=0:d=1[v1];[v0][v1]concat=n=2:v=1:a=0" \
  output.mp4

# Transición wipe
ffmpeg -i v1.mp4 -i v2.mp4 -filter_complex \
  "[0:v][1:v]xfade=transition=wipeleft:duration=1:offset=5" output.mp4
```

### Crear thumbnails automáticamente

```sh
# Thumbnail cada 10 segundos
ffmpeg -i input.mp4 -vf "fps=1/10" thumb_%03d.jpg

# Thumbnail del segundo 30
ffmpeg -i input.mp4 -ss 30 -frames:v 1 thumbnail.jpg

# Thumbnail con texto
ffmpeg -i input.mp4 -ss 10 -frames:v 1 \
  -vf "drawtext=text='Preview':fontsize=48:fontcolor=red:x=(w-tw)/2:y=(h-th)/2" \
  thumbnail.jpg

# Contact sheet (múltiples thumbnails en una imagen)
ffmpeg -i input.mp4 -vf "fps=1/30,scale=320:-1,tile=4x4" contact_sheet.jpg

# Thumbnail de cada minuto
ffmpeg -i input.mp4 -vf "fps=1/60,scale=640:-1" thumb_%04d.jpg
```

### Crear slideshow con música

```sh
# Slideshow de imágenes con música (5 segundos por imagen)
ffmpeg -loop 1 -i img1.jpg -loop 1 -i img2.jpg -loop 1 -i img3.jpg \
  -i musica.mp3 -filter_complex \
  "[0:v]scale=1920:1080:force_original_aspect_ratio=decrease,pad=1920:1080:(ow-iw)/2:(oh-ih)/2,setsar=1[v0]; \
   [1:v]scale=1920:1080:force_original_aspect_ratio=decrease,pad=1920:1080:(ow-iw)/2:(oh-ih)/2,setsar=1[v1]; \
   [2:v]scale=1920:1080:force_original_aspect_ratio=decrease,pad=1920:1080:(ow-iw)/2:(oh-ih)/2,setsar=1[v2]; \
   [v0][v1][v2]concat=n=3:v=1:a=0[v]" \
  -map "[v]" -map 2:a -shortest -c:v libx264 -c:a aac output.mp4

# Slideshow con transiciones de fade
ffmpeg -loop 1 -t 5 -i img1.jpg -loop 1 -t 5 -i img2.jpg -loop 1 -t 5 -i img3.jpg \
  -filter_complex "[0:v][1:v][2:v]concat=n=3:v=1:a=0[v]" -map "[v]" -c:v libx264 output.mp4
```

### Extraer frames como imágenes

```sh
# Extraer frame cada segundo
ffmpeg -i input.mp4 -vf fps=1 frame_%04d.jpg

# Extraer frame cada 10 segundos
ffmpeg -i input.mp4 -vf fps=1/10 frame_%04d.jpg

# Extraer solo 60 frames (uniforme)
ffmpeg -i input.mp4 -vf "select=not(mod(n\,100))" -vsync vfr frame_%04d.jpg

# Extraer frame específico (ejemplo: frame 1000)
ffmpeg -i input.mp4 -vf "select=eq(n\,1000)" -frames:v 1 frame.jpg

# Extraer frames como PNG (calidad sin pérdida)
ffmpeg -i input.mp4 -vf fps=1 frame_%04d.png
```

### Normalización de color (LUT)

```sh
# Aplicar LUT de color (ejemplo: cinematic)
ffmpeg -i input.mp4 -vf "lut3d=cinematic.cube" output.mp4

# Aplicar LUT y ajustar brillo/contraste
ffmpeg -i input.mp4 -vf "lut3d=cinematic.cube,eq=brightness=0.05:contrast=1.1" output.mp4

# Aplicar efecto vintage
ffmpeg -i input.mp4 -vf "curves=vintage" output.mp4

# Blanco y negro
ffmpeg -i input.mp4 -vf "hue=s=0" output.mp4

# Sepia
ffmpeg -i input.mp4 -vf "colorchannelmixer=.393:.769:.189:0:.349:.686:.168:0:.272:.534:.131" output.mp4

# Efecto película antigua
ffmpeg -i input.mp4 -vf "curves=vintage,noise=alls=20:allf=t+u" output.mp4
```

### Stream a Twitch/YouTube Live

```sh
# Stream a Twitch
ffmpeg -i input.mp4 -c:v libx264 -preset veryfast -b:v 3000k \
  -c:a aac -b:a 128k -f flv "rtmp://live.twitch.tv/app/TU_STREAM_KEY"

# Stream a YouTube Live
ffmpeg -i input.mp4 -c:v libx264 -preset veryfast -b:v 4500k \
  -c:a aac -b:a 128k -f flv "rtmp://a.rtmp.youtube.com/live2/TU_STREAM_KEY"

# Stream webcam a Twitch
ffmpeg -f v4l2 -i /dev/video0 -f pulse -i default \
  -c:v libx264 -preset veryfast -b:v 2500k \
  -c:a aac -b:a 128k -f flv "rtmp://live.twitch.tv/app/TU_STREAM_KEY"

# Stream pantalla a YouTube
ffmpeg -f x11grab -i :0.0 -f pulse -i default \
  -c:v libx264 -preset veryfast -b:v 3000k \
  -c:a aac -b:a 128k -f flv "rtmp://a.rtmp.youtube.com/live2/TU_STREAM_KEY"
```

## 14. Casos de uso específicos

### WhatsApp (límite 16MB)

```sh
# Comprimir para WhatsApp (máximo 16MB)
ffmpeg -i input.mp4 -c:v libx264 -b:v 800k -c:a aac -b:a 64k \
  -fs 16M -movflags +faststart whatsapp.mp4

# Script para detectar duración y ajustar bitrate
#!/bin/bash
INPUT="$1"
DURATION=$(ffprobe -v error -show_entries format=duration -of csv=p=0 "$INPUT" | cut -d. -f1)
# 16MB = 131072 kbits, restamos audio (64k * duración)
VIDEO_BITRATE=$(( (131072 - (64 * DURATION)) / DURATION ))
ffmpeg -i "$INPUT" -c:v libx264 -b:v "${VIDEO_BITRATE}k" -c:a aac -b:a 64k \
  -movflags +faststart "whatsapp_${INPUT}"
```

### Telegram (límite 2GB)

```sh
# Comprimir para Telegram (máximo 2GB)
ffmpeg -i input.mp4 -c:v libx264 -crf 28 -preset fast \
  -c:a aac -b:a 128k -movflags +faststart telegram.mp4

# Para máxima compresión (calidad aceptable)
ffmpeg -i input.mp4 -c:v libx264 -crf 32 -preset fast \
  -c:a aac -b:a 96k -movflags +faststart telegram_small.mp4
```

### Instagram

```sh
# Instagram Feed (máximo 60 segundos, 1080x1080)
ffmpeg -i input.mp4 -ss 0 -t 60 -vf "scale=1080:1080:force_original_aspect_ratio=decrease,pad=1080:1080:(ow-iw)/2:(oh-ih)/2" \
  -c:v libx264 -b:v 3500k -c:a aac -b:a 128k instagram_feed.mp4

# Instagram Reels (9:16, máximo 90 segundos)
ffmpeg -i input.mp4 -ss 0 -t 90 -vf "scale=1080:1920:force_original_aspect_ratio=decrease,pad=1080:1920:(ow-iw)/2:(oh-ih)/2" \
  -c:v libx264 -b:v 3500k -c:a aac -b:a 128k instagram_reel.mp4

# Instagram Stories (9:16, máximo 15 segundos)
ffmpeg -i input.mp4 -ss 0 -t 15 -vf "scale=1080:1920:force_original_aspect_ratio=decrease,pad=1080:1920:(ow-iw)/2:(oh-ih)/2" \
  -c:v libx264 -b:v 3500k -c:a aac -b:a 128k instagram_story.mp4
```

### TikTok

```sh
# TikTok (9:16, máximo 3 minutos)
ffmpeg -i input.mp4 -ss 0 -t 180 -vf "scale=1080:1920:force_original_aspect_ratio=decrease,pad=1080:1920:(ow-iw)/2:(oh-ih)/2" \
  -c:v libx264 -b:v 3000k -c:a aac -b:a 128k tiktok.mp4

# TikTok con texto superpuesto
ffmpeg -i input.mp4 -vf "scale=1080:1920:force_original_aspect_ratio=decrease,pad=1080:1920:(ow-iw)/2:(oh-ih)/2,drawtext=text='Mi TikTok':fontsize=36:fontcolor=white:x=(w-tw)/2:y=50" \
  -c:v libx264 -b:v 3000k -c:a aac -b:a 128k tiktok_texto.mp4
```

### YouTube

```sh
# YouTube (recomendado 1080p o 4K)
ffmpeg -i input.mp4 -c:v libx264 -crf 18 -preset slow \
  -c:a aac -b:a 192k -movflags +faststart youtube_1080p.mp4

# YouTube 4K
ffmpeg -i input.mp4 -c:v libx264 -crf 18 -preset slow \
  -vf scale=3840:2160 -c:a aac -b:a 256k -movflags +faststart youtube_4k.mp4

# YouTube Shorts (9:16, máximo 60 segundos)
ffmpeg -i input.mp4 -ss 0 -t 60 -vf "scale=1080:1920:force_original_aspect_ratio=decrease,pad=1080:1920:(ow-iw)/2:(oh-ih)/2" \
  -c:v libx264 -crf 18 -c:a aac -b:a 128k youtube_short.mp4
```

### Twitter/X

```sh
# Twitter (máximo 140 segundos, 1280x720 máximo)
ffmpeg -i input.mp4 -ss 0 -t 140 -vf "scale=1280:720:force_original_aspect_ratio=decrease,pad=1280:720:(ow-iw)/2:(oh-ih)/2" \
  -c:v libx264 -b:v 5000k -c:a aac -b:a 128k twitter.mp4
```

### Backup de canales de YouTube

```sh
#!/bin/bash
# Script para backup de canal de YouTube

CANAL_URL="$1"
BACKUP_DIR="$HOME/Backups/YouTube/$(date +%Y-%m-%d)"
mkdir -p "$BACKUP_DIR"

# Descargar canal completo con metadata
yt-dlp \
  -f "bestvideo[height<=1080]+bestaudio/best" \
  --merge-output-format mp4 \
  --embed-thumbnail \
  --embed-metadata \
  --write-description \
  --write-info-json \
  -o "$BACKUP_DIR/%(upload_date>%Y-%m-%d)s-%(title)s.%(ext)s" \
  --yes-playlist \
  "$CANAL_URL/videos"

echo "Backup completado en: $BACKUP_DIR"
```

### Organizar biblioteca de vídeos

```sh
#!/bin/bash
# Script para organizar vídeos descargados

CARPETA="$1"
OUTPUT="$HOME/Videos/Organizados"
mkdir -p "$OUTPUT"

for file in "$CARPETA"/*.mp4; do
    # Extraer metadata
    TITLE=$(ffprobe -v error -show_entries format_tags=title -of csv=p=0 "$file" 2>/dev/null)
    DATE=$(ffprobe -v error -show_entries format_tags=date -of csv=p=0 "$file" 2>/dev/null)
    
    if [ -z "$TITLE" ]; then
        TITLE=$(basename "$file" .mp4)
    fi
    
    if [ -z "$DATE" ]; then
        DATE="SinFecha"
    fi
    
    # Crear carpeta por año-mes
    CARPETA_DEST="$OUTPUT/$DATE"
    mkdir -p "$CARPETA_DEST"
    
    # Mover archivo
    mv "$file" "$CARPETA_DEST/$TITLE.mp4"
    echo "Movido: $TITLE → $CARPETA_DEST"
done
```

## 15. Scripts completos de automatización

### Script de monitoreo de descargas

```sh
#!/bin/bash
# Monitorear carpeta y descargar automáticamente

CARPETA="$HOME/Downloads/pendientes"
LOG="$HOME/Downloads/log_descargas.txt"

echo "Monitoreando: $CARPETA" | tee -a "$LOG"

while true; do
    for file in "$CARPETA"/*.url; do
        if [ -f "$file" ]; then
            URL=$(cat "$file")
            echo "$(date): Descargando: $URL" | tee -a "$LOG"
            
            yt-dlp \
              -f "bv+ba/b" \
              --merge-output-format mp4 \
              -o "$HOME/Videos/%(channel)s/%(title)s.%(ext)s" \
              --embed-thumbnail \
              --embed-metadata \
              "$URL"
            
            if [ $? -eq 0 ]; then
                echo "$(date): Completado: $URL" | tee -a "$LOG"
                rm "$file"
            else
                echo "$(date): ERROR: $URL" | tee -a "$LOG"
            fi
        fi
    done
    sleep 60
done
```

### Script de compresión automática

```sh
#!/bin/bash
# Comprimir vídeos nuevos automáticamente

CARPETA="$HOME/Videos/para_comprimir"
OUTPUT="$HOME/Videos/comprimidos"
mkdir -p "$OUTPUT"

inotifywait -m -r -e create "$CARPETA" |
while read path action file; do
    if [[ "$file" == *.mp4 ]]; then
        echo "Comprimiendo: $file"
        
        # Esperar a que el archivo esté listo
        sleep 5
        
        ffmpeg -i "$path$file" \
          -c:v libx264 -crf 28 -preset fast \
          -c:a aac -b:a 128k \
          -movflags +faststart \
          "$OUTPUT/$file"
        
        if [ $? -eq 0 ]; then
            echo "Completado: $file"
            rm "$path$file"
        else
            echo "ERROR: $file"
        fi
    fi
done
```

### Script de backup automático

```sh
#!/bin/bash
# Backup automático de canales de YouTube

CANALES=(
    "https://www.youtube.com/@canal1"
    "https://www.youtube.com/@canal2"
    "https://www.youtube.com/@canal3"
)

BACKUP_DIR="$HOME/Backups/YouTube"
LOG="$BACKUP_DIR/log.txt"

mkdir -p "$BACKUP_DIR"

for canal in "${CANALES[@]}"; do
    NOMBRE=$(echo "$canal" | sed 's/.*@//')
    CARPETA="$BACKUP_DIR/$NOMBRE"
    mkdir -p "$CARPETA"
    
    echo "$(date): Descargando: $NOMBRE" | tee -a "$LOG"
    
    yt-dlp \
      -f "bestvideo[height<=1080]+bestaudio/best" \
      --merge-output-format mp4 \
      --download-archive "$CARPETA/.archive" \
      -o "$CARPETA/%(upload_date>%Y-%m-%d)s-%(title)s.%(ext)s" \
      --yes-playlist \
      "$canal/videos"
    
    echo "$(date): Completado: $NOMBRE" | tee -a "$LOG"
done

echo "$(date): Backup general completado" | tee -a "$LOG"
```

### Cron para ejecución automática

```sh
# Editar crontab
crontab -e

# Añadir estas líneas:

# Backup diario a las 3 AM
0 3 * * * /home/usuario/scripts/backup_youtube.sh

# Comprimir vídeos nuevos cada hora
0 * * * * /home/usuario/scripts/comprimir_nuevos.sh

# Limpiar caché de yt-dlp semanalmente (domingos a las 4 AM)
0 4 * * 0 rm -rf ~/.cache/yt-dlp/*
```

### Systemd service para monitoreo

```ini
# /etc/systemd/system/video-monitor.service

[Unit]
Description=Monitor de vídeos para comprimir
After=network.target

[Service]
Type=simple
User=usuario
ExecStart=/home/usuario/scripts/comprimir_nuevos.sh
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Activar:

```sh
sudo systemctl enable video-monitor
sudo systemctl start video-monitor
```
