---
draft: false
title: "🏆 RETO: Reto Avanzado 14: Worker Queue con Prioridad"
description: "Implementa un sistema de cola de trabajos con prioridad donde los trabajos con mayor prioridad se ejecutan primero (max-heap)."
pubDate: "2026-06-28"
tags: ["retos", "avanzado", "multilenguaje"]
slug: "reto-avanzado-14-worker-queue-con-prioridad"
image: "img/guia_plus_reto_avanzado_14_worker_queue_con_prioridad_cover-1200.webp"
author: "Jorge Beneyto Castelló"
difficulty: "Avanzado"
languages: ["python", "javascript", "java", "typescript"]
---

import Challenge from '@components/Challenge.astro';
import CodeTabs from '@components/CodeTabs.svelte';

# 🎯 Desafío: Reto Avanzado 14: Worker Queue con Prioridad

### 📝 Descripción del Reto
Implementa un sistema de cola de trabajos con prioridad donde los trabajos con mayor prioridad se ejecutan primero (max-heap).

<Challenge 
  nivel="Avanzado" 
  mision="Implementa un sistema de cola de trabajos con prioridad donde los trabajos con mayor prioridad se ejecutan primero..." 
/>

---

### 🧪 Casos de Prueba

| Entrada | Salida esperada |
|---------|-----------------|
| `add('A',3),add('B',1),execute()` | `'A'` |
| `add('X',5),execute()` | `'X'` |
| `execute()` | `error (empty)` |
| `add('A',1),add('B',1),execute()` | `'A' (FIFO en empate)` |

---

## 💡 Guía de Solución Paso a Paso

<details>
<summary><b>Ver explicación y código 🛠️ (¡No hagas spoiler!)</b></summary>
<div class="details-content">

### 🏗️ Paso 1: Análisis de la lógica
**Análisis del problema:** Una cola de prioridad es un heap máximo: el elemento con mayor prioridad está siempre en la raíz. Operaciones: add(tarea, prioridad), execute() → ejecuta la de mayor prioridad y la elimina. **Ejemplo concreto:** add('A',3), add('B',1), add('C',2) → execute() → 'A' (prio 3), execute() → 'C' (prio 2). **Edge cases:** cola vacía, prioridades iguales (orden FIFO), tareas sin prioridad, muchas tareas con la misma prioridad.

### ⚙️ Paso 2: Implementación
**Implementación:** Usar un heap (max-heap) implementado con lista. Las operaciones principales: insert (push al final y subir/burbujear hacia arriba), extract_max (intercambiar raíz con último, bajar/burbujear hacia abajo). En Python, heapq es min-heap: se usa prioridad negativa para simular max-heap.

### 🚀 Paso 3: Complejidad y Optimización

**Complejidad temporal:** O(log n)  
**Complejidad espacial:** O(n)  

**Complejidad:** Inserción y extracción: O(log n) cada una. **Aplicaciones:** schedulers de SO, planificación de procesos, sistemas de colas de mensajes (RabbitMQ), procesamiento por lotes prioritario, algoritmos de Dijkstra/Prim.

### 💻 Código de la Solución

<CodeTabs client:load>

```python
import heapq
class PQ:
 def __init__(self): self.q=[]
 def add(self, t, p): heapq.heappush(self.q, (-p, t))
 def get(self): return heapq.heappop(self.q)[1]
```

```javascript
// Reto Avanzado 14: Worker Queue con Prioridad
// Implementa una solución para: Reto Avanzado 14: Worker Queue con Prioridad

function resolver(entrada) {
  return entrada;
}

console.log(resolver('ejemplo'));

```

```java
// Reto Avanzado 14: Worker Queue con Prioridad
// Implementa una solución para: Reto Avanzado 14: Worker Queue con Prioridad

public class Reto {
    public static String resolver(String entrada) {
        return entrada;
    }

    public static void main(String[] args) {
        System.out.println(resolver("ejemplo"));
    }
}

```

```typescript
// Reto Avanzado 14: Worker Queue con Prioridad
// Implementa una solución para: Reto Avanzado 14: Worker Queue con Prioridad

function resolver<T>(entrada: T): T {
  return entrada;
}

console.log(resolver('ejemplo'));

```

</CodeTabs>

</div>
</details>
