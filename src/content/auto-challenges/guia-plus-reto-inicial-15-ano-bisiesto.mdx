---
draft: false
title: "🏆 RETO: Reto Inicial 15: Año Bisiesto"
description: "Determina si un año es bisiesto según las reglas del calendario gregoriano: divisible entre 4, excepto si es divisible entre 100, a menos que sea divisible..."
pubDate: "2026-06-28"
tags: ["retos", "iniciaci\u00f3n", "multilenguaje"]
slug: "reto-inicial-15-ano-bisiesto"
image: "img/guia_plus_reto_inicial_15_ano_bisiesto/guia_plus_reto_inicial_15_ano_bisiesto_cover-1200.webp"
author: "Jorge Beneyto Castelló"
difficulty: "Iniciación"
languages: ["python", "javascript", "java", "typescript"]
---

import Challenge from '@components/Challenge.astro';
import CodeTabs from '@components/CodeTabs.svelte';

# 🎯 Desafío: Reto Inicial 15: Año Bisiesto

### 📝 Descripción del Reto
Determina si un año es bisiesto según las reglas del calendario gregoriano: divisible entre 4, excepto si es divisible entre 100, a menos que sea divisible entre 400.

<Challenge 
  nivel="Iniciación" 
  mision="Determina si un año es bisiesto según las reglas del calendario gregoriano: divisible entre 4, excepto si es divisible..." 
/>

---

### 🧪 Casos de Prueba

| Entrada | Salida esperada |
|---------|-----------------|
| `2024` | `true` |
| `2023` | `false` |
| `1900` | `false` |
| `2000` | `true` |
| `2400` | `true` |

---

## 💡 Guía de Solución Paso a Paso

<details>
<summary><b>Ver explicación y código 🛠️ (¡No hagas spoiler!)</b></summary>
<div class="details-content">

### 🏗️ Paso 1: Análisis de la lógica
**Análisis del problema:** Un año es bisiesto si cumple: (divisible entre 4 Y no entre 100) O (divisible entre 400). Es una lógica booleana con tres condiciones. **Ejemplo concreto:** 2024 → sí (divisible entre 4, no entre 100), 1900 → no (divisible entre 100, no entre 400), 2000 → sí (divisible entre 400). **Edge cases:** años negativos (según convención), año 0, años muy grandes.

### ⚙️ Paso 2: Implementación
**Implementación:** Se evalúa la condición compuesta: `(año % 4 == 0 and año % 100 != 0) or (año % 400 == 0)`. Es un clásico para practicar operadores lógicos y precedencia.

### 🚀 Paso 3: Complejidad y Optimización

**Complejidad temporal:** O(1)  
**Complejidad espacial:** O(1)  

**Complejidad:** O(1) tiempo y espacio. **Aplicaciones:** sistemas de calendario, cálculo de días entre fechas, planificación de eventos recurrentes.

### 💻 Código de la Solución

<CodeTabs client:load>

```python
def sol(y): return (y % 4 == 0 and y % 100 != 0) or (y % 400 == 0)
print(sol(2024))
```

```javascript
// Reto Inicial 15: Año Bisiesto
// Implementa una solución para: Reto Inicial 15: Año Bisiesto

function resolver(entrada) {
  return entrada;
}

console.log(resolver('ejemplo'));

```

```java
// Reto Inicial 15: Año Bisiesto
// Implementa una solución para: Reto Inicial 15: Año Bisiesto

public class Reto {
    public static String resolver(String entrada) {
        return entrada;
    }

    public static void main(String[] args) {
        System.out.println(resolver("ejemplo"));
    }
}

```

```typescript
// Reto Inicial 15: Año Bisiesto
// Implementa una solución para: Reto Inicial 15: Año Bisiesto

function resolver<T>(entrada: T): T {
  return entrada;
}

console.log(resolver('ejemplo'));

```

</CodeTabs>

</div>
</details>
