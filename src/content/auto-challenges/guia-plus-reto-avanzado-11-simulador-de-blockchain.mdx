---
draft: false
title: "🏆 RETO: Reto Avanzado 11: Simulador de Blockchain"
description: "Implementa una cadena de bloques simple con validación de integridad mediante hashes SHA-256 y prueba de trabajo (proof of work)."
pubDate: "2026-06-28"
tags: ["retos", "avanzado", "multilenguaje"]
slug: "reto-avanzado-11-simulador-de-blockchain"
image: "img/reto-avanzado-11-simulador-de-blockchain_cover-1200.webp"
author: "Jorge Beneyto Castelló"
difficulty: "Avanzado"
languages: ["python", "javascript", "java", "typescript"]
---

import Challenge from '@components/Challenge.astro';
import CodeTabs from '@components/CodeTabs.svelte';

# 🎯 Desafío: Reto Avanzado 11: Simulador de Blockchain

### 📝 Descripción del Reto
Implementa una cadena de bloques simple con validación de integridad mediante hashes SHA-256 y prueba de trabajo (proof of work).

<Challenge 
  nivel="Avanzado" 
  mision="Implementa una cadena de bloques simple con validación de integridad mediante hashes SHA-256 y prueba de trabajo (proof..." 
/>

---

### 🧪 Casos de Prueba

| Entrada | Salida esperada |
|---------|-----------------|
| `crearCadena(), addBloque('tx1')` | `valida` |
| `crearCadena(), modificarBloque(0)` | `invalida` |
| `addBloque(''), addBloque('abc')` | `2 bloques` |

---

## 💡 Guía de Solución Paso a Paso

<details>
<summary><b>Ver explicación y código 🛠️ (¡No hagas spoiler!)</b></summary>
<div class="details-content">

### 🏗️ Paso 1: Análisis de la lógica
**Análisis del problema:** Cada bloque contiene datos, su propio hash, el hash del bloque anterior, y un nonce (para proof of work). La cadena es válida si cada bloque apunta al hash correcto del anterior. **Ejemplo concreto:** Bloque 1: 'genesis', hash=abc... → Bloque 2: 'tx1', prev=abc..., hash=def... Si alguien modifica el bloque 1, su hash cambia y el bloque 2 queda inválido. **Edge cases:** cadena vacía (solo bloque genesis), bloque con datos vacíos, nonce que nunca encuentra solución, hash collisions.

### ⚙️ Paso 2: Implementación
**Implementación:** Clase Bloque con índice, timestamp, datos, prev_hash, nonce, hash. Método mine_block(dificultad) que incrementa nonce hasta que el hash empiece por 'difficulty' ceros. Clase Blockchain con lista de bloques y método is_chain_valid.

### 🚀 Paso 3: Complejidad y Optimización

**Complejidad temporal:** O(1) inserción, O(n) verificación  
**Complejidad espacial:** O(n)  

**Complejidad:** Inserción/propagación de bloques: O(1). Verificación de cadena: O(n). Minería (proof of work): exponencial en la dificultad — O(16^difficulty). **Aplicaciones:** criptomonedas, certificación de documentos, registros inmutables, trazabilidad en supply chain.

### 💻 Código de la Solución

<CodeTabs client:load>

```python
import hashlib
def hash_b(b): return hashlib.sha256(str(b).encode()).hexdigest()
def mine(b, d): 
 while not hash_b(b).startswith('0'*d): b['nonce']+=1
 return b
```

```javascript
// Reto Avanzado 11: Simulador de Blockchain
// Implementa una solución para: Reto Avanzado 11: Simulador de Blockchain

function resolver(entrada) {
  return entrada;
}

console.log(resolver('ejemplo'));

```

```java
// Reto Avanzado 11: Simulador de Blockchain
// Implementa una solución para: Reto Avanzado 11: Simulador de Blockchain

public class Reto {
    public static String resolver(String entrada) {
        return entrada;
    }

    public static void main(String[] args) {
        System.out.println(resolver("ejemplo"));
    }
}

```

```typescript
// Reto Avanzado 11: Simulador de Blockchain
// Implementa una solución para: Reto Avanzado 11: Simulador de Blockchain

function resolver<T>(entrada: T): T {
  return entrada;
}

console.log(resolver('ejemplo'));

```

</CodeTabs>

</div>
</details>
