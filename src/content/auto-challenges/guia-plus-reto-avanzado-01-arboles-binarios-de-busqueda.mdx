---
draft: false
title: "🏆 RETO: Reto Avanzado 01: Árboles Binarios de Búsqueda"
description: "Implementa un Árbol Binario de Búsqueda (BST) con inserción y búsqueda. Cada nodo tiene un valor, hijo izquierdo (menor) y derecho (mayor)."
pubDate: "2026-06-28"
tags: ["retos", "avanzado", "multilenguaje"]
slug: "reto-avanzado-01-arboles-binarios-de-busqueda"
image: "img/guia_plus_reto_avanzado_01_arboles_binarios_de_busqueda/guia_plus_reto_avanzado_01_arboles_binarios_de_busqueda_cover-1200.webp"
author: "Jorge Beneyto Castelló"
difficulty: "Avanzado"
languages: ["python", "javascript", "java", "typescript"]
---

import Challenge from '@components/Challenge.astro';
import CodeTabs from '@components/CodeTabs.svelte';

# 🎯 Desafío: Reto Avanzado 01: Árboles Binarios de Búsqueda

### 📝 Descripción del Reto
Implementa un Árbol Binario de Búsqueda (BST) con inserción y búsqueda. Cada nodo tiene un valor, hijo izquierdo (menor) y derecho (mayor).

<Challenge 
  nivel="Avanzado" 
  mision="Implementa un Árbol Binario de Búsqueda (BST) con inserción y búsqueda. Cada nodo tiene un valor, hijo izquierdo..." 
/>

---

### 🧪 Casos de Prueba

| Entrada | Salida esperada |
|---------|-----------------|
| `insertar(5,3,7,1,9), buscar(7)` | `true` |
| `insertar(5,3,7), buscar(4)` | `false` |
| `insertar(), buscar(1)` | `false` |
| `insertar(1,2,3), buscar(3)` | `true` |

---

## 💡 Guía de Solución Paso a Paso

<details>
<summary><b>Ver explicación y código 🛠️ (¡No hagas spoiler!)</b></summary>
<div class="details-content">

### 🏗️ Paso 1: Análisis de la lógica
**Análisis del problema:** Un BST es una estructura donde para cada nodo, todos los valores del subárbol izquierdo son menores y los del derecho son mayores. Esto permite búsqueda binaria O(log n). **Ejemplo concreto:** insertar 5, 3, 7, 1, 9. Buscar 7 → encontrado. Buscar 4 → no encontrado. **Edge cases:** árbol vacío (root=null), valores duplicados (según implementación), inserción secuencial (degenera a lista), árbol muy profundo.

### ⚙️ Paso 2: Implementación
**Implementación:** Clase Nodo con val, left, right. Insertar: si root es None, crear nodo. Si valor &lt; root.val, insertar en left; si &gt;, en right. Buscar: similar, comparando en cada nivel.

### 🚀 Paso 3: Complejidad y Optimización

**Complejidad temporal:** O(log n) promedio, O(n) peor caso  
**Complejidad espacial:** O(n)  

**Complejidad:** O(log n) promedio, O(n) peor caso (árbol desbalanceado). O(n) espacio para almacenar n nodos. Mejorable con árboles AVL o Red-Black. **Aplicaciones:** búsquedas dinámicas, diccionarios en memoria, implementación de sets, sistemas de archivos.

### 💻 Código de la Solución

<CodeTabs client:load>

```python
class Node:
 def __init__(self,v): self.v=v; self.l=None; self.r=None
def ins(root,v):
 if not root: return Node(v)
 if v<root.v: root.l=ins(root.l,v)
 else: root.r=ins(root.r,v)
 return root
```

```javascript
// Reto Avanzado 01: Árboles Binarios de Búsqueda
// Implementa una solución para: Reto Avanzado 01: Árboles Binarios de Búsqueda

function resolver(entrada) {
  return entrada;
}

console.log(resolver('ejemplo'));

```

```java
// Reto Avanzado 01: Árboles Binarios de Búsqueda
// Implementa una solución para: Reto Avanzado 01: Árboles Binarios de Búsqueda

public class Reto {
    public static String resolver(String entrada) {
        return entrada;
    }

    public static void main(String[] args) {
        System.out.println(resolver("ejemplo"));
    }
}

```

```typescript
// Reto Avanzado 01: Árboles Binarios de Búsqueda
// Implementa una solución para: Reto Avanzado 01: Árboles Binarios de Búsqueda

function resolver<T>(entrada: T): T {
  return entrada;
}

console.log(resolver('ejemplo'));

```

</CodeTabs>

</div>
</details>
---
## Retos relacionados

- [Grafos (BFS/DFS)](/retos/reto-avanzado-02-grafos-bfs-dfs/)
- [Motor de Búsqueda Binaria](/retos/reto-motor-de-busqueda-binaria/)

