---
draft: false
title: "🏆 RETO: Reto Intermedio 30: Manejador de Historial (Undo)"
description: "Implementa un sistema de historial con capacidad de deshacer (undo) y rehacer (redo) usando dos pilas."
pubDate: "2026-06-28"
tags: ["retos", "intermedio", "multilenguaje"]
slug: "reto-intermedio-30-manejador-de-historial-undo"
image: "img/guia_plus_reto_intermedio_30_manejador_de_historial_undo_cover-1200.webp"
author: "Jorge Beneyto Castelló"
difficulty: "Intermedio"
languages: ["python", "javascript", "java", "typescript"]
---

import Challenge from '@components/Challenge.astro';
import CodeTabs from '@components/CodeTabs.svelte';

# 🎯 Desafío: Reto Intermedio 30: Manejador de Historial (Undo)

### 📝 Descripción del Reto
Implementa un sistema de historial con capacidad de deshacer (undo) y rehacer (redo) usando dos pilas.

<Challenge 
  nivel="Intermedio" 
  mision="Implementa un sistema de historial con capacidad de deshacer (undo) y rehacer (redo) usando dos pilas." 
/>

---

### 🧪 Casos de Prueba

| Entrada | Salida esperada |
|---------|-----------------|
| `add('a'),add('b'),undo()` | `'a'` |
| `add('x'),undo(),redo()` | `'x'` |
| `undo()` | `error` |
| `add('a'),add('b'),undo(),add('c'),redo()` | `'ac'` |

---

## 💡 Guía de Solución Paso a Paso

<details>
<summary><b>Ver explicación y código 🛠️ (¡No hagas spoiler!)</b></summary>
<div class="details-content">

### 🏗️ Paso 1: Análisis de la lógica
**Análisis del problema:** Se mantienen dos pilas: una de acciones realizadas y otra de acciones deshechas. Al hacer undo, la acción actual pasa a la pila de redo. Al hacer una acción nueva, se vacía la pila de redo. **Ejemplo concreto:** escribir('a'), escribir('b'), undo() → 'a', redo() → 'ab'. **Edge cases:** undo sin acciones, redo sin acciones, capacidad máxima de historial.

### ⚙️ Paso 2: Implementación
**Implementación:** Clase Historial con métodos: add(accion), undo(), redo(). Usar dos listas como pilas (append/pop). El estado actual se reconstruye aplicando todas las acciones de la pila de undo.

### 🚀 Paso 3: Complejidad y Optimización

**Complejidad temporal:** O(1)  
**Complejidad espacial:** O(n)  

**Complejidad:** Todas las operaciones O(1). O(n) espacio para el historial. **Aplicaciones:** editores de texto, IDEs, sistemas de diseño, editores de imágenes, terminales.

### 💻 Código de la Solución

<CodeTabs client:load>

```python
class Hist:
 def __init__(self): self.do, self.re = [], []
 def add(self, x): self.do.append(x); self.re = []
 def undo(self): 
  if self.do: self.re.append(self.do.pop())
 def redo(self): 
  if self.re: self.do.append(self.re.pop())
```

```javascript
// Reto Intermedio 30: Manejador de Historial (Undo)
// Implementa una solución para: Reto Intermedio 30: Manejador de Historial (Undo)

function resolver(entrada) {
  return entrada;
}

console.log(resolver('ejemplo'));

```

```java
// Reto Intermedio 30: Manejador de Historial (Undo)
// Implementa una solución para: Reto Intermedio 30: Manejador de Historial (Undo)

public class Reto {
    public static String resolver(String entrada) {
        return entrada;
    }

    public static void main(String[] args) {
        System.out.println(resolver("ejemplo"));
    }
}

```

```typescript
// Reto Intermedio 30: Manejador de Historial (Undo)
// Implementa una solución para: Reto Intermedio 30: Manejador de Historial (Undo)

function resolver<T>(entrada: T): T {
  return entrada;
}

console.log(resolver('ejemplo'));

```

</CodeTabs>

</div>
</details>
