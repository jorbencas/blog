---
draft: false
title: "Reto Avanzado 19: Compresión Huffman"
description: "Genera el árbol de Huffman para un texto dado."
pubDate: "2025-11-17"
tags: ["python", "retos", "avanzado", "guia-plus"]
slug: "guia-plus-reto-avanzado-19-compresion-huffman"
image: "/img/guia_plus_reto_avanzado_19_compresion_huffman_cover-1200.webp"
author: "Jorge Beneyto Castelló"
difficulty: "Avanzado"
---

import Challenge from '../../components/Challenge.astro';

# 🎯 Reto Avanzado 19: Compresión Huffman

### 📝 Descripción del Reto

Genera el árbol de Huffman para un texto dado.

<Challenge 
  nivel="Avanzado" 
  mision="Genera el árbol de Huffman para un texto dado." 
/>

---

## 💡 Guía de Solución Paso a Paso

<details>
<summary><b>Ver explicación y código 🛠️ (¡No hagas spoiler!)</b></summary>
<div class="details-content">

### 🏗️ Paso 1: Análisis de la lógica

Contar frecuencias, construir nodos y usar heap para unir los dos menores.

### ⚙️ Paso 2: Implementación en Python

Árbol binario de prefijos.

### 🚀 Paso 3: Complejidad y Optimización

O(n log n).

### 💻 Código de la Solución (Python)

```python
import heapq
def sol(txt):
 from collections import Counter
 f = Counter(txt); pq = [[v, [k, '']] for k,v in f.items()]
 heapq.heapify(pq)
 while len(pq) > 1:
  lo = heapq.heappop(pq); hi = heapq.heappop(pq)
  for p in lo[1:]: p[1] = '0' + p[1]
  for p in hi[1:]: p[1] = '1' + p[1]
  heapq.heappush(pq, [lo[0] + hi[0]] + lo[1:] + hi[1:])
 return sorted(heapq.heappop(pq)[1:], key=lambda p: (len(p[-1]), p))
```

</div>
</details>
