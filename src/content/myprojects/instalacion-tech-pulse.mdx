---
draft: false
title: "Instalación de Tech Pulse: Dashboard de Noticias Tech con IA"
description: "Guía completa para instalar y configurar Tech Pulse: scraping de 515 fuentes, resúmenes con Gemini, notificaciones Telegram, newsletter Mailgun, tips de IT y herramientas IA."
pubDate: "2026/09/04"
updatedDate: "2026/09/04"
tags: ["python", "ia", "web-scraping", "instalación", "github-actions", "telegram"]
image: "img/instalacion_tech_pulse/instalacion_tech_pulse_cover-1200.webp"
author: "Jorge Beneyto Castelló"
---

Tech Pulse es un pipeline completo de automatización: scraping de 515 fuentes, resúmenes con IA, notificaciones Telegram, newsletter por email, tips de IT y herramientas IA. Pero para que funcione, necesitas configurar varias API keys. Cada una tiene su purpose y te explico cómo obtenerlas.

---

## 1. Por qué Tech Pulse (y no otra cosa)

| Herramienta | Qué hace | Limitación |
|-------------|----------|------------|
| **Feedly** | Lee RSS | No filtra por relevancia, no genera resúmenes IA |
| **Google News** | Noticias tech | No scraping directo, no personalizable |
| **Herramientas manuales** | Scraping a medida | Requieren mantenimiento constante |
| **Tech Pulse** | Scraping + IA + notificaciones + dashboard | Todo automatizado, 515 fuentes, 134 tests |

**¿Por qué Python?** Ecosistema de scraping (requests, beautifulsoup), IA (google-generativeai), flexibilidad para integrar todo en un solo pipeline.

**¿Por qué Gemini?** Gratis tier generoso, buena calidad en español, resúmenes naturales. Fallback a Ollama/Qwen local si falla.

**¿Por qué GitHub Actions?** CI/CD gratis, cron integrado, secrets seguros, despliegue automático a Surge.sh/Vercel.

---

## 2. Requisitos previos

| Requisito | Para qué | Cómo obtenerlo |
|-----------|----------|----------------|
| **Python 3.11+** | Lenguaje principal | [python.org/downloads](https://www.python.org/downloads/) |
| **Git** | Clonar el repo | [git-scm.com](https://git-scm.com/) |
| **API Key de Gemini** (recomendado) | Resúmenes IA, tips, herramientas | [Google AI Studio](https://aistudio.google.com/) — explicado abajo |
| **Token de Telegram** (opcional) | Notificaciones | [@BotFather](https://t.me/BotFather) — explicado abajo |
| **API Key de Mailgun** (opcional) | Newsletter email | [mailgun.com](https://www.mailgun.com/) — explicado abajo |
| **API Key de Unsplash** (opcional) | Imágenes para posts | [unsplash.com/developers](https://unsplash.com/developers) |

---

## 3. Obtener la API Key de Gemini

**Qué es:** Una clave que le da acceso a tu script a los modelos de IA de Google (Gemini). Se usa para generar resúmenes de noticias, tips de programación y herramientas IA.

**Cómo obtenerla:**

1. Ve a [aistudio.google.com/apikey](https://aistudio.google.com/apikey)
2. Inicia sesión con tu cuenta de Google
3. Haz clic en **"Create API Key"**
4. Selecciona un proyecto (o crea uno nuevo)
5. Copia la API key que empieza por `AIza...`

**¿Es gratis?** Sí, el tier gratuito permite 1500 requests/día. Tech Pulse hace ~100 requests/día, así que sobra.

**¿Qué pasa si no la configuro?** El sistema usa fallback: primero intenta Gemini, si falla usa una base de datos local de definiciones. Los tips y herramientas funcionan igual, pero con menos variedad.

---

## 4. Obtener el token de Telegram

**Qué es:** La "contraseña" de tu bot de Telegram. Se usa para enviar notificaciones de noticias.

**Cómo obtenerlo:**

1. Abre Telegram y busca [@BotFather](https://t.me/BotFather)
2. Envía `/newbot`
3. Elige un nombre (ej: "Tech Pulse Bot")
4. Elige un username que termine en `bot` (ej: `techpulse_bot`)
5. Copia el token

**¿Y el chat_id?** Es el ID del canal o grupo donde quieres recibir notificaciones. Para obtenerlo:

```bash
# Añadir el bot al grupo, luego:
curl "https://api.telegram.org/botTU_TOKEN/getUpdates" | jq '.result[].message.chat.id'
```

O simplemente envía un mensaje al bot y mira la respuesta de `getUpdates`.

---

## 5. Obtener la API Key de Mailgun

**Qué es:** Un servicio de envío de emails transaccionales. Se usa para enviar la newsletter diaria con resúmenes de noticias.

**Cómo obtenerla:**

1. Ve a [signup.mailgun.com](https://signup.mailgun.com/)
2. Crea una cuenta (gratis: 100 emails/día durante 30 días)
3. En el dashboard, ve a **Sending** → **API Keys**
4. Copia la API key (empieza por `key-...`)
5. En **Domains**, verifica un dominio (o usa el dominio de prueba)

**¿Es indispensable?** No. Si no configuras Mailgun, el newsletter no se envía pero el resto funciona (dashboard, Telegram, tips).

---

## 6. Obtener la API Key de Unsplash

**Qué es:** Un servicio de imágenes gratuitas. Se usa para poner imágenes en los posts del blog.

**Cómo obtenerla:**

1. Ve a [unsplash.com/developers](https://unsplash.com/developers)
2. Crea una cuenta
3. Haz clic en **"New Application"**
4. Acepta los términos
5. Copia la **Access Key**

**¿Es indispensable?** No. Sin ella, los posts no tienen imágenes pero el contenido funciona igual.

---

## 7. Clonar y configurar

```bash
# 1. Clonar el repo
git clone https://github.com/jorbencas/test_githubActions.git
cd test_githubActions

# 2. Crear entorno virtual
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# .venv\Scripts\activate   # Windows

# 3. Instalar dependencias
pip install -r requirements.txt
```

---

## 8. Configurar variables de entorno

Crea el archivo `.env` en la raíz del proyecto:

```bash
cat > .env << EOF
# Gemini API (recomendado — para resúmenes y tips)
GEMINI_API_KEY=AIzaSy...tu_api_key

# Telegram (opcional — para notificaciones)
TELEGRAM_BOT_TOKEN=8580222159:AAE4e21gzeq6q...
TELEGRAM_CHAT_ID=-1004332325883

# Mailgun (opcional — para newsletter email)
MAILGUN_API_KEY=key-...tu_api_key
MAILGUN_DOMAIN=tudominio.com
EMAIL_USER=tu@email.com

# Unsplash (opcional — para imágenes)
UNSPLASH_ACCESS_KEY=...tu_access_key
EOF
```

### Por qué cada variable

| Variable | Para qué | Si no la pones |
|----------|----------|----------------|
| `GEMINI_API_KEY` | Resúmenes IA, tips, herramientas | Fallback a DB local (funciona pero con menos variedad) |
| `TELEGRAM_BOT_TOKEN` | Enviar notificaciones | Notificaciones Telegram deshabilitadas |
| `TELEGRAM_CHAT_ID` | Destino de notificaciones | Notificaciones Telegram deshabilitadas |
| `MAILGUN_API_KEY` | Enviar newsletter email | Email deshabilitado |
| `MAILGUN_DOMAIN` | Dominio de envío | Email deshabilitado |
| `EMAIL_USER` | Email destino | Email deshabilitado |
| `UNSPLASH_ACCESS_KEY` | Imágenes en posts | Posts sin imágenes |

### Dónde va el archivo

En la raíz de `test_githubActions/`:

```
test_githubActions/
├── .env                    ← AQUÍ
├── requirements.txt
├── scripts/
└── ...
```

---

## 9. Probar el scraping

```bash
# Scraping completo (todas las 515 fuentes — tarda ~30 min)
python -m scripts.scrapers.scrape_news --tier full

# Scraping ligero (solo quick sources, ~15 min)
python -m scripts.scrapers.scrape_news --tier light

# Scrapear herramientas de GitHub
python -m scripts.scrapers.scrape_tools
```

**¿Qué genera?** Archivos JSON en `files/`:
- `noticias_historico.json` — Todas las noticias scrapeadas
- `herramientas.json` — Herramientas de GitHub

**¿Cuánto tarda?** El scraping completo tarda ~30 minutos. El ligero ~15 minutos. Es normal porque hay 515 fuentes que consultar.

---

## 10. Generar el dashboard

```bash
# Generar solo el HTML del dashboard
python -m scripts.publishers.generate_weekly --dashboard-only

# Previsualizar
python -m http.server -d public
# Abrir http://localhost:8000 en el navegador
```

**¿Qué genera?** Un archivo `public/index.html` con el dashboard completo: noticias, vídeos, ranking GitHub, filtros combinables.

---

## 11. Probar notificaciones

```bash
# Telegram (sin enviar — dry-run)
python -m scripts.publishers.send_telegram --dry-run --max-items 3

# Email (sin enviar — dry-run)
python -m scripts.publishers.send_email --dry-run --max-items 5
```

**¿Qué hace el dry-run?** Muestra lo que enviaría sin enviar nada. Así puedes verificar que la configuración es correcta antes de enviar de verdad.

---

## 12. Generar tips y herramientas

```bash
# Ver categorías de tips disponibles
python scripts/tips_generator.py --list-categories

# Previsualizar tips
python scripts/tips_generator.py --dry-run

# Generar herramientas IA
python scripts/ai_tools_generator.py --dry-run
```

**¿Qué son los tips?** 283 conceptos de programación con preguntas de entrevista. Se generan con Gemini + Wikipedia + fallback local.

**¿Qué son las herramientas IA?** 150 categorías de herramientas de IA con auto-detección de GitHub, Product Hunt y HuggingFace.

---

## 13. Ejecutar tests

```bash
# Todos los tests (134 tests)
python -m pytest tests/ -v

# Tests específicos
python -m pytest tests/test_solutions_db.py -v
```

**¿Por qué tests?** Porque el pipeline tiene muchos scripts que se ejecutan en cadena. Si uno falla, el resto falla. Los tests detectan problemas antes de que lleguen a producción.

---

## 14. Uso en GitHub Actions (recomendado)

El proyecto está diseñado para ejecutarse automáticamente via GitHub Actions. Los workflows ya están configurados:

| Workflow | Frecuencia | Qué hace |
|----------|------------|----------|
| `scrape_hourly.yml` | Cada hora | Scraping de quick sources |
| `scrape_6h.yml` | Cada 6 horas | Scraping de standard sources |
| `daily_tips.yml` | Cada 3 horas | Generar tips de IT |
| `daily_ai_tools.yml` | Cada 3 horas | Generar herramientas IA |
| `send_email.yml` | Diario (09:00 UTC) | Enviar newsletter |
| `send_telegram.yml` | Cada 30 min | Enviar noticias a Telegram |

### Configurar GitHub Secrets

En tu fork del repo, ve a **Settings** → **Secrets and variables** → **Actions** y añade:

| Secret | Valor |
|--------|-------|
| `GEMINI_API_KEY` | Tu API key de Gemini |
| `TELEGRAM_BOT_TOKEN` | Token del bot |
| `TELEGRAM_CHAT_ID` | ID del chat/grupo |
| `MAILGUN_API_KEY` | API key de Mailgun |
| `MAILGUN_DOMAIN` | Dominio de Mailgun |
| `EMAIL_USER` | Email destino |

**¿Por qué GitHub Secrets y no `.env`?** Porque GitHub Actions no puede leer archivos `.env`. Los secrets se inyectan como variables de entorno durante la ejecución del workflow.

---

## 15. Verificación final

```bash
# Comprobar que el scraping funciona
ls -la files/noticias_historico.json

# Comprobar que el dashboard se genera
ls -la public/index.html

# Comprobar que los tests pasan
python -m pytest tests/ -v --tb=short
```

---

## Troubleshooting

| Problema | Causa | Solución |
|----------|-------|----------|
| `ModuleNotFoundError` | Falta instalar dependencias | `pip install -r requirements.txt` con venv activado |
| `GEMINI_API_KEY not set` | No configurada | Añadir al `.env` o configurar en GitHub Secrets |
| `Rate limit exceeded` | Demasiadas peticiones | Esperar o reducir frecuencia de scraping |
| `Telegram send failed` | Token o chat_id incorrecto | Verificar en `.env` o GitHub Secrets |
| `Connection refused` (Mailgun) | API key incorrecta | Verificar en dashboard de Mailgun |
| `No module named 'bs4'` | Falta beautifulsoup | `pip install beautifulsoup4` |

---

¿Quieres ver cómo funciona el ecosistema completo? Consulta:

- [Instalación del Ecosistema Devjobs: Pipeline Completo desde Cero](/myprojects/instalacion-devjobs)
- [Instalación de FFmpeg + yt-dlp: Conversor de Vídeo](/myprojects/instalacion-ffmpeg)
- [Instalación de Telegram Toolbox: Bot y Uploader](/myprojects/instalacion-telegram)
- [Tech Pulse: Dashboard Automatizado de Noticias Tech](/myprojects/tech-pulse-dashboard)
